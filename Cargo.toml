[package]
name = "org-files-db"
version = "0.1.0"
edition = "2021"

[dependencies]
orgize = "0.10.0-alpha.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"

[lib]
name = "org_files_db"
path = "src/lib.rs"

[[bin]]
name = "org-files-db"
path = "src/main.rs"
