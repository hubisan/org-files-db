#+TITLE: Query SQL Performance
#+STARTUP: showall

* Purpose

This document records the SQL performance work for structural query execution and result enrichment.

The uncached presentation benchmark showed that query execution dominates latency for large result sets.

The SQL work keeps public query semantics and normal JSON output unchanged.

* Round 1 baseline and result

The first uncached presentation benchmark measured these database-query medians at ~50'000~ original results.

| Workload | Original median |
|----------+-----------------|
| ~headings.normal~ | about ~798 ms~ |
| ~headings.tags~ | about ~824 ms~ |
| ~headings.effective-properties~ | about ~1.25 s~ |
| ~files.keywords~ | about ~1.04 s~ |

Those measurements used fixed ~900~-ID enrichment chunks.

Round 1 replaced the fixed chunk size with the runtime SQLite variable limit. It also removed broad reloads after simple flat queries.

The local round 1 benchmark reported ~SQLITE_LIMIT_VARIABLE_NUMBER = 32766~ and these ~50'000~-result medians.

| Workload | Round 1 median | SQL statements |
|----------+----------------+----------------|
| ~headings.normal~ | ~354.2 ms~ | 5 |
| ~headings.tags~ | ~352.1 ms~ | 5 |
| ~headings.properties~ | ~481.1 ms~ | 7 |
| ~headings.effective-properties~ | ~492.2 ms~ | 7 |
| ~headings.path~ | ~846.0 ms~ | 11 |
| ~files.normal~ | ~179.3 ms~ | 5 |
| ~files.keywords~ | ~361.2 ms~ | 7 |

The common presentation workloads improved by about ~56%~ to ~65%~ against the original database-query baseline.

The round 1 lookup microbenchmark also compared five ways to load ~150'000~ effective-property rows for ~50'000~ heading IDs.

| Strategy | Median |
|----------+--------|
| ~query-derived-join~ | ~23.7 ms~ |
| ~json-each-relation~ | ~39.5 ms~ |
| ~runtime-limit-in~ | ~58.0 ms~ |
| ~temporary-id-relation~ | ~74.7 ms~ |
| ~cte-values~ | ~102.9 ms~ |

The query-derived join won at every measured result size.

A temporary relation lost the single-lookup test because the test included relation setup cost. Round 2 adds a reuse benchmark across several enrichment stages.

* Round 1 SQL changes

** Runtime variable limit

Large ID lists use the connection value of ~SQLITE_LIMIT_VARIABLE_NUMBER~.

The code derives the available parameter capacity from that runtime limit.

A connection with a smaller configured limit uses smaller chunks automatically.

File-restriction inserts also use the runtime parameter capacity.

** Direct flat shaping

Flat heading rows already contain heading scalars, file paths, locations, and effective tags.

Flat file rows contain file scalars and the synthetic-root fields required by public results.

Flat link rows contain link scalars, source locations, and file paths.

Round 1 shapes simple flat rows directly and avoids broad file and heading reloads.

** Query joins and ordering

Top-level heading queries always join ~files~ because public heading rows contain the file path.

They join the synthetic root only when a predicate uses root data.

Nested relations add file and root joins only when their predicates require those aliases.

Global SQL sorting was removed where Rust can restore the same public order with small grouped sorts.

* Round 2 architecture

Round 2 targets the remaining SQLite to Rust to SQLite ID round trips.

The production query executor now keeps the compiled matched SQL relation beside the normal matched rows.

This relation metadata stays internal. Public query rows and public JSON output keep their existing shapes.

The production execute-and-shape path passes the internal relation to result shaping.

Standalone shaping from caller-supplied ~QueryRows~ keeps the existing ID-based fallback loaders.

** Query snapshot

Public query execution now starts one deferred read transaction when the caller has no active transaction.

All match and enrichment statements for that call use the same SQLite snapshot.

An existing caller transaction remains authoritative and is not nested.

An error rolls back the transaction that the query call created.

** Heading tags

Base heading execution still returns effective tags as part of each heading row.

The tag loader no longer sends matched heading IDs back through a dynamic ~IN (...)~ list.

It wraps the exact compiled heading query as a ~matched~ CTE and joins ~effective_tags~ to that relation.

Tag order remains stored-position order after a small Rust sort per heading.

** Direct metadata enrichment

The production direct-flat path uses query-derived relations for:

- direct properties
- effective properties
- keywords
- file-root tags
- ~outline_path~ validation

Each relation uses the exact compiled match query and its original bound parameters.

Rust keeps the existing property, keyword, and tag ordering after collection.

A relation loader skips heading or root branches when the matched result has no rows from that branch.

The old runtime-limit ID loaders remain available for data sets that originate in Rust.

** Path enrichment

Flat heading path output no longer uses the recursive query-derived CTE in production.

The production loader reuses matched heading rows that are already in Rust.

Rust collects missing parent IDs and loads only required ancestors with runtime-limit-safe ID batches.

The loader validates ~outline_path~ rows for matched headings and loaded ancestors.

Rust then constructs the final file and heading path from the matched rows and loaded ancestors.

The current missing-row and malformed-JSON errors remain unchanged.

The recursive query-derived loader remains available as a benchmark reference.

Flat file path output needs only the matched file and its synthetic-root data.

Complex link, backlink, source, target, and outline contexts keep the broader enrichment path.

Those contexts can introduce file-wide relations and one-to-many fan-out. They remain on the safe fallback without new benchmark evidence.

** Avoid join fan-out

Round 2 does not combine unrelated one-to-many relations into one large result query.

Tags, properties, keywords, and links remain separate enrichment relations.

This avoids multiplication such as ~tag_count * property_count~ for one heading.

* Round 2 strategy benchmark

Benchmark output schema version ~2~ keeps the round 1 lookup comparison and adds relation-reuse measurements.

The reuse benchmark compares:

- repeated query-derived joins
- one shared TEMP result relation reused by several loaders

It uses both a simple match relation and a more selective complex match relation.

The simple relation matches level-1 headings.

The complex relation also requires the ~project~ effective tag and the ~GROUP=group0~ effective property.

Each relation has two workloads.

- The single workload loads effective properties.
- The multi workload loads effective properties, effective tags, direct properties, and ~outline_path~ rows.

The shared TEMP timing includes relation reset and population on every sample.

The repeated-derived timing includes re-evaluation of the matched relation for every loader.

Both strategies run inside one deferred transaction so they use one stable database snapshot.

This comparison shows whether one reusable relation amortizes its setup cost across several enrichments.

** Historical round 2 local result

This section records the earlier pre-predicate-driven relation-reuse result. The schema version ~10~ selective-reuse evidence below supersedes it for the current production selector.

The supplied round 2 run uses one benchmark environment for every strategy comparison.

Repeated query-derived joins win every measured simple-match workload.

The shared TEMP relation wins every measured complex-match workload.

At ~50'000~ target results, the simple multi-enrichment median is ~56.8 ms~ for repeated query-derived joins and ~92.6 ms~ for the shared TEMP relation.

At the same size, the complex multi-enrichment median is ~367.8 ms~ for repeated query-derived joins and ~104.7 ms~ for the shared TEMP relation.

This result does not support one global relation-reuse strategy.

Keep repeated query-derived joins for cheap matched relations.

Treat selective TEMP relation reuse for expensive matched relations as a separate follow-up.

* Selective matched-relation reuse

The predicate-driven metadata compiler changes the cost of the matched heading relation, so the relation-reuse benchmark now compiles the simple and complex workloads through the current production compiler instead of using hand-written benchmark SQL.

Benchmark output schema version ~10~ keeps the isolated relation-reuse comparison and adds a complete production A/B between query-derived execution and selective TEMP reuse.

The isolated comparison includes the full TEMP lifecycle in every sample:

- reset any previous benchmark TEMP relation
- create the TEMP table
- populate it from the exact compiled matched-heading relation
- run the downstream loaders
- drop the TEMP table

It records matched heading count, rows returned to Rust, statement count, SQLite profile stages, and the query plan of the compiled matched relation.

The production A/B first requires exact public result equality, then measures complete query latency and the direct phase profile for cheap and expensive relations with single and multiple enrichment workloads.

** Schema version 10 relation-reuse result

The supplied schema version ~10~ run uses the current production compiler at ~100~, ~1'000~, ~10'000~, and ~50'000~ target results.

The isolated relation-reuse medians are:

| Target results | Simple single: derived | Simple single: TEMP | Simple multi: derived | Simple multi: TEMP | Complex single: derived | Complex single: TEMP | Complex multi: derived | Complex multi: TEMP |
|----------------+------------------------+---------------------+-----------------------+--------------------+-------------------------+----------------------+------------------------+---------------------|
| 100 | 0.146 ms | 0.227 ms | 0.536 ms | 0.360 ms | 0.177 ms | 0.259 ms | 0.704 ms | 0.319 ms |
| 1'000 | 0.636 ms | 1.013 ms | 2.162 ms | 2.309 ms | 0.518 ms | 0.806 ms | 2.033 ms | 1.389 ms |
| 10'000 | 7.013 ms | 10.967 ms | 24.355 ms | 26.601 ms | 5.647 ms | 8.792 ms | 22.211 ms | 15.968 ms |
| 50'000 | 37.935 ms | 59.579 ms | 130.842 ms | 141.935 ms | 32.969 ms | 52.064 ms | 130.250 ms | 95.124 ms |

The refreshed isolated benchmark changes the interpretation of the old schema version ~9~ result.

TEMP does not win every workload that contains the complex tag-plus-property relation.

The complex single-loader workload remains faster query-derived at every measured size.

The complex multi-loader workload remains faster with TEMP at every measured size, with a ~27.0%~ advantage at ~50'000~ target results.

The simple multi-loader workload also shows that consumer count alone is not a stable selector: TEMP wins at ~100~ target results but loses at ~1'000~, ~10'000~, and ~50'000~.

Use the complete production A/B as the authoritative selector evidence because production always includes the initial matched-row read and required relation-backed validation and enrichment in addition to explicitly requested includes.

** Schema version 10 production A/B

The production benchmark compares the same public query with selective TEMP reuse disabled and enabled.

Exact complete ~QueryResponse~ equality is required before timing.

The medians are:

| Target results | Workload | Query-derived | Selective production | Change | TEMP selected |
|----------------+----------+---------------+----------------------+--------+---------------|
| 100 | cheap single | 1.003 ms | 1.005 ms | +0.1% | no |
| 100 | cheap multi | 1.447 ms | 1.455 ms | +0.5% | no |
| 100 | expensive single | 0.945 ms | 0.436 ms | -53.9% | yes |
| 100 | expensive multi | 1.153 ms | 0.530 ms | -54.0% | yes |
| 1'000 | cheap single | 6.850 ms | 6.870 ms | +0.3% | no |
| 1'000 | cheap multi | 9.449 ms | 9.537 ms | +0.9% | no |
| 1'000 | expensive single | 2.411 ms | 1.981 ms | -17.8% | yes |
| 1'000 | expensive multi | 3.581 ms | 2.895 ms | -19.2% | yes |
| 10'000 | cheap single | 84.264 ms | 85.399 ms | +1.3% | no |
| 10'000 | cheap multi | 111.587 ms | 111.059 ms | -0.5% | no |
| 10'000 | expensive single | 27.407 ms | 23.793 ms | -13.2% | yes |
| 10'000 | expensive multi | 40.275 ms | 35.826 ms | -11.0% | yes |
| 50'000 | cheap single | 481.312 ms | 496.198 ms | +3.1% | no |
| 50'000 | cheap multi | 602.482 ms | 604.321 ms | +0.3% | no |
| 50'000 | expensive single | 157.770 ms | 133.787 ms | -15.2% | yes |
| 50'000 | expensive multi | 230.889 ms | 200.708 ms | -13.1% | yes |

The measured production result supports the current static selector.

Every expensive workload selects TEMP and improves complete query latency at every measured size.

This includes the single-include workload, so the selector does not need a minimum downstream consumer count.

The expensive query matches only ~7~, ~76~, ~769~, and ~3'846~ headings as the target corpus grows, but the selector does not use those result counts.

Every cheap workload remains query-derived.

At ~100~ and ~1'000~ target results, the selective path differs from the disabled path by at most ~0.9%~, so the measured small-workload comparison shows no material regression.

The ~50'000~ cheap-single medians differ by ~3.1%~ even though both records report ~temp_selected=false~ and use the same query-derived strategy. With three timing samples, that pair does not provide evidence of a TEMP-induced regression.

For the ~50'000~ expensive single workload, TEMP materialization costs about ~33.8 ms~, but complete latency still falls from ~157.8 ms~ to ~133.8 ms~ because repeated relation-backed SQL work is reduced.

For the ~50'000~ expensive multi workload, materialization costs about ~34.0 ms~, while complete latency falls from ~230.9 ms~ to ~200.7 ms~.

The schema version ~10~ result therefore supports keeping the conservative two-metadata-relation selector without runtime calibration.

** Production selection rule

Production classifies the validated heading predicate tree before execution.

The initial production classification uses only the deterministic compiled-query shape that has direct benchmark evidence:

- ~tags~, ~property~, and ~keyword~ count as predicate-driven metadata relations because each adds a relation-driven membership lookup to the matched relation.
- nested queries are traversed recursively so predicate-driven metadata relations compiled inside them still contribute to the outer matched-relation cost.
- Boolean nesting is traversed but does not by itself make a relation expensive; cheap scalar predicates inside a deep Boolean tree do not justify TEMP setup.

A matched heading relation is ~expensive~ when it contains at least two predicate-driven metadata relations.

All other matched heading relations are ~cheap~.

Correlated hierarchy, link, timestamp, body-text, and outline subqueries are plausible future expensive signals. They are deliberately not TEMP-selected by the initial rule because the supplied benchmark evidence does not isolate those shapes. They should be added only after the refreshed benchmark demonstrates a complete-query win.

The rule intentionally does not use result count, database size, or downstream consumer count.

This permits an expensive relation to select TEMP with one enrichment consumer and permits a cheap relation to stay query-derived with several consumers.

** Production TEMP lifecycle

When a relation is expensive, production materializes the exact compiled heading relation into one connection-local TEMP table before the first heading-row read.

The initial heading rows, effective-tag loading, outline validation, and applicable metadata enrichments then read the same parameter-free TEMP-backed relation.

The synthetic-root file relation remains query-derived.

The TEMP table is dropped before the public query returns. Cleanup also runs after shaping failures and after failed TEMP population, and a reset precedes every materialization.

TEMP state is therefore scoped to one database connection and one complete public query and cannot be reused by a later independent query.

* ID transport fallback

Round 1 showed that ~json_each~ beats large dynamic ~IN (...)~ lists on the benchmark system.

The production fallback still uses runtime-limit ~IN (...)~ chunks.

This path works without a project requirement for SQLite JSON table functions.

Round 2 keeps ~json_each~ in the benchmark for IDs that genuinely originate in Rust.

A later change can use it when measurements justify the extra serialization and SQLite JSON parsing.

* Query plan audit

Benchmark output schema version ~2~ also records ~EXPLAIN QUERY PLAN~ details from the real compiled query SQL.

The audit covers representative queries for:

- heading level
- heading title
- file path
- direct tags
- effective tags
- direct properties
- effective properties
- keywords
- parent hierarchy
- link source
- link target
- scheduled time
- file restriction

The level-1 benchmark intentionally matches a large fraction of the heading table. A table scan can be the correct plan for this broad case.

Exact normalized heading titles can use ~idx_headings_title_lower~.

Exact normalized file paths can use ~idx_files_path_lower~.

Direct tag and property lookups have dedicated indexes.

Effective tag and property predicates use their existing materialized projections.

Parent, link source, link target, and planning timestamp predicates have dedicated indexes.

The benchmark records the actual plan details so local results can expose unexpected scans or temporary B-trees.

Round 2 adds no persistent index before those plans and timings provide evidence.

** Predicate-driven metadata strategy benchmark

Benchmark output schema version ~6~ adds ~metadata_predicate_strategies~ for every result size.

The benchmark covers direct tags, direct properties, effective properties, and keywords.

Each metadata family uses approximately 1%, 10%, 50%, and 100% selectivity.

The benchmark creates isolated copies of the normal corpus database.

Synthetic marker rows exist only in these copies.

Each selectivity uses a separate marker so every SQL shape returns the same deterministic level-1 heading IDs.

The benchmark compares three strategies.

- ~heading-driven-exists~ matches the current compiler shape.
- ~relation-driven-current-indexes~ starts from the metadata relation and uses the production indexes.
- ~relation-driven-experimental-index~ uses the same relation-first SQL on a copy with benchmark-only predicate-first indexes.

The relation-first SQL uses ~CROSS JOIN~ so SQLite keeps the metadata relation as the outer loop.

Direct property candidates use ~effective_properties.local_value~.

This matches the current compiler semantics after local duplicate and append resolution.

The experimental indexes are:

- ~tags(tag, heading_id)~
- ~effective_properties(key, local_value, heading_id) WHERE local_value IS NOT NULL~
- ~effective_properties(key, effective_value, heading_id)~
- ~keywords(keyword COLLATE NOCASE, value, heading_id)~

These indexes exist only in the benchmark database copy.

The production schema remains unchanged.

Each strategy result records:

- total one-statement latency through Rust ID collection
- requested and actual selectivity
- matched and returned heading rows
- rows transferred to Rust
- statement count
- ~EXPLAIN QUERY PLAN~ details
- the benchmark-only index name when applicable

The current benchmark API does not expose exact scanned-row counts.

The ~rows_scanned~ field is therefore null.

Use the query plan and returned-row count to interpret scan behavior.

Schema version ~6~ also adds a top-level ~compiler_audit~.

The metadata audit finds no safe new lookup skip based only on heading level or metadata mode.

Direct tags and local properties can exist on root and regular headings.

Effective properties can exist wherever a property key is visible.

Direct and inherited keyword modes use different heading identities.

The existing heading-root static truth logic already skips guaranteed impossible root work.

** Predicate-driven metadata benchmark result

The local schema version ~6~ benchmark used up to ~50'000~ eligible headings.

All three strategies return identical heading IDs before timing.

Each sample uses one SQL statement and transfers only matched heading IDs to Rust.

At ~50'000~ headings, the median times are:

| Predicate | Selectivity | Heading ~EXISTS~ | Relation, current indexes | Relation, experimental index |
|-----------+-------------+------------------+---------------------------+------------------------------|
| direct tag | 1% | 20.490 ms | 0.171 ms | 0.144 ms |
| direct tag | 10% | 21.066 ms | 1.558 ms | 1.285 ms |
| direct tag | 50% | 22.949 ms | 9.156 ms | 7.846 ms |
| direct tag | 100% | 25.477 ms | 18.807 ms | 16.904 ms |
| direct property | 1% | 24.318 ms | 17.484 ms | 0.157 ms |
| direct property | 10% | 25.556 ms | 19.542 ms | 1.364 ms |
| direct property | 50% | 32.655 ms | 25.543 ms | 8.112 ms |
| direct property | 100% | 42.090 ms | 33.907 ms | 16.675 ms |
| effective property | 1% | 23.719 ms | 17.190 ms | 0.152 ms |
| effective property | 10% | 25.242 ms | 18.665 ms | 1.347 ms |
| effective property | 50% | 32.494 ms | 25.212 ms | 8.237 ms |
| effective property | 100% | 41.341 ms | 33.543 ms | 16.639 ms |
| keyword | 1% | 25.866 ms | 11.906 ms | 0.159 ms |
| keyword | 10% | 26.572 ms | 13.350 ms | 1.496 ms |
| keyword | 50% | 32.164 ms | 20.158 ms | 8.741 ms |
| keyword | 100% | 41.488 ms | 29.140 ms | 17.871 ms |

The relation-driven current-index strategy beats the heading-driven strategy in every measured case.

The experimental predicate-first strategy is the fastest strategy in every measured case.

Direct tags already use ~idx_tags_tag~ in the relation-driven current-index plan.

The covering tag index adds only a smaller incremental gain.

Do not add another persistent tag index from this result.

The current property relation-first plans still scan ~effective_properties~.

The experimental property indexes replace the scan with a covering predicate-first search.

The current keyword relation-first plan still scans ~keywords~.

The experimental keyword index replaces the scan with a covering predicate-first search.

The benchmark does not include later enrichment or final result shaping.

Require a complete production query A/B before adding the property or keyword indexes to the production schema.

** Complete production metadata A/B

Benchmark output schema version ~7~ adds ~production_metadata_predicate_strategies~.

This section runs only for result sizes of at least ~10'000~ headings.

It reuses the isolated metadata benchmark database copies.

The current strategy keeps correlated ~EXISTS~ predicates.

The candidate strategy uses an equivalent ~heading_id IN (SELECT ...)~ predicate for non-regexp metadata equality.

This form keeps the surrounding Boolean expression unchanged.

It therefore preserves ~and~, ~or~, ~not~, and nested expression composition at the predicate boundary.

The candidate remains benchmark-only until the end-to-end result supports a production change.

Direct tags use the existing production indexes in this comparison.

The benchmark does not test another persistent tag index.

Direct properties use ~effective_properties.local_value~.

Effective properties use ~effective_properties.effective_value~.

Keywords preserve the current case-insensitive key match and exact value match.

For properties and keywords, the indexed candidate runs on the benchmark-only predicate-first index copy.

Before timing, the benchmark requires identical complete ~QueryResponse~ values.

Each retained timing record includes:

- complete production query latency
- final result count
- SQL profile summary
- direct phase profile
- rows transferred from SQLite
- statement and parameter counts
- the compiled match query plan
- the benchmark-only index name where applicable

The complete timing includes normal match execution, row decoding, required enrichment, Rust grouping, sorting, and final shaping.

Do not add the property or keyword indexes to ~sql/schema.sql~ before these results are reviewed.

* Existing materialized projections

The schema already stores several expensive derived relations.

- ~effective_properties~ stores resolved property values.
- ~effective_tags~ stores inherited tag order.
- ~outline_path~ stores structural outline paths.
- Optional FTS stores indexed title and body search data.

Round 2 adds no persistent materialized table.

The current bottleneck is access to matched relations, not recomputation of these existing projections.

A new persistent projection needs measured benefit that exceeds rebuild, update, storage, and schema costs.

* Production profiling

Production SQL statements contain internal benchmark comments with a stage name and parameter count.

The measurement binary enables SQLite profiling only for one separate profile run per workload.

Latency samples run without the profile callback.

The profile records actual statement counts, bound parameter counts, and SQLite-reported statement durations.

The SQLite profile callback has coarse timing resolution on the benchmark system.

Complete workload timings and dedicated strategy timings remain the authoritative latency measurements.

* Rust and SQLite boundary benchmark

Benchmark output schema version ~3~ adds a separate direct phase profile for each production workload.

The normal latency samples do not enable direct phase instrumentation.

One separate instrumented run records the direct phase data.

This separation keeps fine-grained timers out of the authoritative latency samples.

** Direct phases

The direct profile records these phase families when the current loader exposes a useful boundary:

- ~query-compilation~
- ~matched-sql-execution~
- ~enrichment-sql-execution~
- ~sqlite-row-decoding~
- ~rust-grouping~
- ~rust-local-sorting~
- ~outline-validation~
- ~heading-path-construction~
- ~final-result-shaping~

The SQL execution timing includes statement preparation, parameter binding, query execution, and SQLite row stepping.

The row-decoding timing measures Rust extraction from each returned SQLite row.

The direct profile also records statement counts and bound parameter counts where the loader exposes them.

~rows_transferred_from_sqlite~ counts rows returned by recorded matched and enrichment SELECT statements.

A phase-level ~rows~ value is an operation work count. It is not a unique-row count across all phases.

Do not subtract the old SQLite profile duration from total workload time to infer Rust time.

Use the direct phase profile only for attribution. Use ~total_query_time~ for production latency.

** Final result shaping detail

Schema version ~11~ keeps the existing ~final-result-shaping~ phase and adds a separate ~final-result-shaping-detail~ attribution phase for direct-flat queries.

The detail phase records aggregate timings instead of one record per result.

Heading detail operations are:

- ~heading-tags-json~ for ~all_tags_json~ decoding
- ~heading-metadata-attach~ for already loaded path and metadata lookup or transfer into the result
- ~heading-node-build~ for final node construction, including clones from the matched row

File detail operations are:

- ~file-path-components~ for deriving ~name~ and ~dir~ from the stored path
- ~file-metadata-attach~ for tags, path, and optional metadata lookup or transfer
- ~file-node-build~ for final node construction.

Link detail records ~link-node-build~ when the direct-flat link path is used.

These detail timers run only during the existing benchmark trace run. They are not active during authoritative latency samples.

The detail phase is diagnostic. Use it to select a concrete shaping candidate, then use ~total_query_time~ to decide whether to retain that candidate.

The schema version ~10~ ~50'000~-result direct profiles provide the starting evidence for this investigation:

- normal headings: approximately ~44 ms~ final shaping;
- headings with properties: approximately ~57 ms~;
- headings with effective properties: approximately ~58 ms~;
- headings with path: approximately ~49 ms~.

Refresh these workloads with schema version ~11~ before changing production shaping.

#+begin_src sh
target/release/orgfdb-query-sql-benchmark \
  --rows 100,1000,10000,50000 \
  --warmups 1 \
  --iterations 3 \
  --work-dir /tmp/orgfdb-presentation-benchmark-work \
  --output /tmp/orgfdb-query-sql-benchmark-final-shaping-v11.json
#+end_src

Inspect ~final-result-shaping-detail~ operations for ~headings.normal~, ~headings.properties~, ~headings.effective-properties~, and ~headings.path~.

Inspect ~files.normal~ only if the selected candidate changes shared file-result shaping.

*** Schema version 11 local baseline

The schema version ~11~ run was measured under WSL after the benchmark environment changed.

Use this run only as the baseline for later candidate runs in the same WSL environment.

Do not compare its absolute latency with earlier host runs.

The ~50'000~-result direct profile reports:

| Workload | Complete median | Final shaping | Main measured shaping costs |
|----------+-----------------+---------------+-----------------------------|
| ~headings.normal~ | ~263.5 ms~ | ~35.4 ms~ | node build ~16.4 ms~, tags JSON ~9.5 ms~ |
| ~headings.properties~ | ~363.9 ms~ | ~47.4 ms~ | metadata attach ~19.3 ms~, node build ~8.4 ms~ |
| ~headings.effective-properties~ | ~362.5 ms~ | ~103.9 ms~ | metadata attach ~37.1 ms~, node build ~36.9 ms~ |
| ~headings.path~ | ~777.8 ms~ | ~72.1 ms~ | node build ~33.1 ms~, tags JSON ~15.3 ms~ |
| ~files.normal~ | ~164.1 ms~ | ~24.8 ms~ | path components ~6.8 ms~, node build ~6.7 ms~ |
| ~files.keywords~ | ~387.9 ms~ | ~48.9 ms~ | metadata attach ~20.8 ms~, node build ~8.2 ms~ |

The measurements identify one local ownership candidate that affects both heading and file result shaping.

The direct-flat shaper owns ~QueryRows~, but it iterates through row references and clones row-owned strings into final result nodes.

The metadata maps are also local to the direct-flat shaper.

Properties, effective properties, keywords, and root tags are cloned from these maps before the maps are discarded.

Test one candidate that consumes direct-flat rows and removes local metadata entries from their maps.

This candidate must only move values whose ownership is local to one result.

Keep ~all_tags_json~ parsing unchanged because the profile measures it separately and the final public result needs decoded tags.

Use the schema version ~11~ baseline above and a candidate run from the same WSL environment for the decision.

The first ownership candidate run shows the expected direct shaping reductions at ~50'000~ results.

Representative detail changes are:

| Workload | Baseline shaping | Candidate shaping | Main candidate change |
|----------+------------------+-------------------+-----------------------|
| ~headings.normal~ | ~35.4 ms~ | ~31.3 ms~ | node build ~16.4 -> 1.3 ms~ |
| ~headings.properties~ | ~47.4 ms~ | ~32.7 ms~ | metadata attach ~19.3 -> 4.4 ms~ |
| ~headings.effective-properties~ | ~103.9 ms~ | ~34.7 ms~ | metadata attach ~37.1 -> 6.2 ms~, node build ~36.9 -> 1.1 ms~ |
| ~headings.path~ | ~72.1 ms~ | ~51.7 ms~ | node build ~33.1 -> 1.4 ms~ |
| ~files.keywords~ | ~48.9 ms~ | ~31.3 ms~ | metadata attach ~20.8 -> 7.3 ms~, node build ~8.2 -> 1.8 ms~ |

Do not use the complete latency difference between these two separate benchmark runs as retention evidence.

Several unchanged SQL, row-decoding, grouping, and sorting phases also changed materially between the runs.

At ~50'000~ results some unchanged phases differ by approximately ~30-40%~.

This means that the separate-run complete latency change contains run-to-run environment or cache drift in addition to the shaping change.

Schema version ~12~ therefore adds a paired production shaping comparison inside one benchmark process.

For each broad workload it compares:

- ~clone-baseline~, which reproduces the pre-candidate direct-flat clone behavior;
- ~move-owned~, which consumes row-owned strings and removes local metadata vectors from their maps.

The paired benchmark:

- uses the same database, validated query, connection, and transaction;
- requires exact ~QueryResponse~ equality before timing;
- alternates baseline-first and candidate-first sample order;
- records complete query latency for both strategies;
- records one separate direct phase profile for both strategies;
- keeps direct phase instrumentation out of latency samples.

Run:

#+begin_src sh
target/release/orgfdb-query-sql-benchmark \
  --rows 100,1000,10000,50000 \
  --warmups 1 \
  --iterations 3 \
  --work-dir /tmp/orgfdb-presentation-benchmark-work \
  --output /tmp/orgfdb-query-sql-benchmark-final-shaping-paired-v12.json
#+end_src

Inspect ~final_shaping_strategies~ for the retention decision.

Keep the candidate only if the paired complete query latency improves without a material small-workload regression.

If a paired difference is small, rerun with more iterations before deciding.

*** Schema version 12 paired retention result

The schema version ~12~ paired benchmark completed under the same WSL environment as the candidate work.

The benchmark requires exact public ~QueryResponse~ equality before timing and alternates which strategy runs first.

With ~--iterations 3~, each strategy has six paired latency samples per workload.

The median complete-query changes are:

| Workload | ~100~ | ~1'000~ | ~10'000~ | ~50'000~ |
|----------+-------+---------+----------+----------|
| ~headings.normal~ | ~-2.4%~ | ~-14.6%~ | ~-6.8%~ | ~235.1 -> 232.5 ms (-1.1%)~ |
| ~headings.tags~ | ~-8.4%~ | ~-5.5%~ | ~-23.9%~ | ~219.9 -> 212.3 ms (-3.4%)~ |
| ~headings.properties~ | ~-9.5%~ | ~-8.4%~ | ~-14.0%~ | ~342.5 -> 293.3 ms (-14.3%)~ |
| ~headings.effective-properties~ | ~-3.5%~ | ~-13.1%~ | ~-16.0%~ | ~366.1 -> 324.9 ms (-11.3%)~ |
| ~headings.path~ | ~-4.1%~ | ~-2.9%~ | ~-5.6%~ | ~447.1 -> 419.2 ms (-6.2%)~ |
| ~files.normal~ | ~+4.4%~ | ~-7.3%~ | ~-6.1%~ | ~119.1 -> 112.7 ms (-5.3%)~ |
| ~files.keywords~ | ~-4.4%~ | ~-20.9%~ | ~-10.3%~ | ~274.1 -> 241.5 ms (-11.9%)~ |

The only small-workload median regression is ~files.normal~ at ~100~ results: approximately ~0.191 -> 0.199 ms~, an absolute increase of about ~0.008 ms~.

At ~1'000~ results the same workload improves by approximately ~7.3%~.

Treat the ~100~-result difference as non-material.

All seven representative ~50'000~-result workloads improve end to end.

The strongest broad improvements are the metadata-heavy paths: ~headings.properties~ improves by approximately ~14.3%~, ~headings.effective-properties~ by approximately ~11.3%~, and ~files.keywords~ by approximately ~11.9%~.

The separate direct phase profiles also attribute the expected work reduction to the targeted ownership changes.

At ~50'000~ results, representative detail operations change as follows:

- ~headings.properties~ metadata attach: approximately ~12.6 -> 5.0 ms~; node build: approximately ~6.0 -> 1.1 ms~.
- ~headings.effective-properties~ metadata attach: approximately ~14.5 -> 6.1 ms~; node build: approximately ~7.0 -> 1.1 ms~.
- ~files.keywords~ metadata attach: approximately ~14.9 -> 6.7 ms~; node build: approximately ~5.9 -> 1.8 ms~.

Some one-shot direct phase samples still fluctuate in unrelated work and in the complete ~final-result-shaping~ phase.

Do not use those single profiled runs as the retention metric.

The paired complete-query medians are authoritative for this decision.

Retain ~move-owned~ as the production direct-flat shaping strategy.

Do not add a second shaping optimization in this task.

The first local ownership change already provides measurable end-to-end benefit across every representative large workload without a material small-workload regression.

Keep the shaping detail profile and paired shaping strategy benchmark as performance regression tools.

They are measurement-only and remain outside normal latency samples and public query behavior.

** Path strategy experiment

Schema version ~3~ also adds a benchmark-only path strategy comparison.

Both strategies reuse one matched ~headings (level 1)~ result snapshot.

The benchmark reference is ~recursive-query-derived~.

It uses the recursive matched relation and loads only required ancestors.

The production strategy is ~rust-driven-bulk-ancestors~.

It starts from the matched heading rows, collects parent IDs in Rust, and loads missing ancestors in bounded bulk queries.

The Rust-driven strategy uses the runtime SQLite variable limit for every ID batch.

It keeps ~outline_path~ validation for matched headings and loaded ancestors.

The benchmark requires exact path output equality before it records timings.

The path strategy timing starts after the initial matched query. It measures only path loading and construction.

The direct phase profile for each strategy records SQL transfer, row decoding, Rust grouping, validation, and path construction.

The path strategy benchmark first kept production path loading unchanged.

The result-size and variable-limit measurements later justified the Rust-driven production switch.

** Path variable-limit sensitivity

Benchmark output schema version ~4~ extends each path strategy result with SQLite variable-limit metadata.

The benchmark option ~--path-variable-limits~ accepts a comma-separated list of positive limits.

The option affects only the path strategy comparison.

Each requested limit uses a fresh read-only SQLite connection.

The benchmark sets ~SQLITE_LIMIT_VARIABLE_NUMBER~ before it runs the path loaders.

Each path strategy result records:

- ~requested_sqlite_variable_limit~
- ~sqlite_variable_limit~ after SQLite applies the request
- ~id_chunk_capacity~
- timing and direct phase data

SQLite can clamp a requested limit to its compile-time maximum.

The requested and effective values therefore remain separate in the output.

If ~--path-variable-limits~ is absent, the benchmark keeps the previous behavior and measures the connection default once.

** Path variable-limit result

The ~50'000~-heading sensitivity run keeps ~rust-driven-bulk-ancestors~ faster at every tested limit.

- ~250000~: ~339.0 ms~ recursive and ~264.8 ms~ Rust-driven.
- ~32766~: ~343.2 ms~ recursive and ~278.2 ms~ Rust-driven.
- ~999~: ~299.0 ms~ recursive and ~285.5 ms~ Rust-driven.

At ~999~, the Rust-driven loader uses ~102~ statements but still avoids an end-to-end regression.

The Rust-driven loader transfers ~100'000~ rows instead of ~150'000~ rows for all three limits.

This result supports the production switch while keeping runtime-limit-safe chunking.

** Production path rebenchmark baseline

The pre-switch schema version ~3~ run provides the production ~headings.path~ A/B baseline.

It ran on the same local machine and SQLite environment that will run the post-switch benchmark.

| Results | Pre-switch median | SQL statements | Rows transferred |
|---------+-------------------+----------------+------------------|
| 100 | ~1.346579 ms~ | 4 | 700 |
| 1'000 | ~13.033683 ms~ | 4 | 7'000 |
| 10'000 | ~114.541652 ms~ | 4 | 70'000 |
| 50'000 | ~502.262111 ms~ | 4 | 350'000 |

The baseline comes from ~orgfdb-query-sql-benchmark-rust-vs-sqlite-v1.json~.

** Production path rebenchmark result

The second post-switch run is the recorded production result.

| Results | Pre-switch | Post-switch v2 | Change | Rows before | Rows after |
|---------+------------+----------------+--------+-------------+------------|
| 100 | ~1.346579 ms~ | ~0.678259 ms~ | ~-49.6%~ | 700 | 600 |
| 1'000 | ~13.033683 ms~ | ~5.360033 ms~ | ~-58.9%~ | 7'000 | 6'000 |
| 10'000 | ~114.541652 ms~ | ~79.084026 ms~ | ~-31.0%~ | 70'000 | 60'000 |
| 50'000 | ~502.262111 ms~ | ~570.870439 ms~ | ~+13.7%~ | 350'000 | 300'000 |

The first post-switch run reported ~580.245167 ms~ at ~50'000~ results.

The second run reported ~570.870439 ms~ after the benchmark machine stayed idle.

The similar large-result values make the production regression a real follow-up signal.

The same second run still favors the Rust-driven loader in the controlled path strategy section.

| Strategy | 50'000-result median | Rows transferred |
|----------+------------------------+------------------|
| ~recursive-query-derived~ | ~282.251136 ms~ | 150'000 |
| ~rust-driven-bulk-ancestors~ | ~211.825539 ms~ | 100'000 |

The Rust-driven loader is approximately ~25.0%~ faster in that same-process comparison.

The production result does not justify an immediate return to the recursive SQL strategy.

Run one same-process complete production A/B before changing the loader again.

Then optimize the Rust construction and shaping cost if the complete A/B still favors the Rust-driven loader.

At ~50'000~ results, the v2 direct phase profile reports approximately:

#+begin_example
enrichment SQL          117.1 ms
matched SQL              44.1 ms
SQLite row decoding      92.4 ms
Rust grouping            53.6 ms
Rust local sorting       21.4 ms
outline validation       12.3 ms
path construction        61.2 ms
final result shaping     86.2 ms
#+end_example

The direct phase profile comes from a separate instrumented sample.

** Complete production path strategy benchmark

Benchmark output schema version ~5~ adds ~production_path_strategies~ for every result size.

This section measures the complete direct-flat ~headings.path~ composition in one process.

Both strategies run the same match query, tag enrichment, path enrichment, and final result shaping.

The benchmark compares:

- ~recursive-query-derived~
- ~rust-driven-bulk-ancestors~

The benchmark requires exact ~QueryResponse~ equality before it records timings.

Each strategy result records:

- complete query latency
- SQL statement profile
- rows transferred from SQLite
- direct phase timings
- final result shaping time

Production still selects ~rust-driven-bulk-ancestors~.

The alternate strategy exists only for the same-process benchmark reference.

The Rust-driven production path now moves each loaded ~Vec<PathEntry>~ into its result node.

It no longer clones the complete path vector during direct-flat shaping.

The Rust path constructor also reuses one cycle-detection buffer across matched headings.

It builds the common path case directly in one final ~Vec<PathEntry>~.

Run the schema version ~5~ benchmark before accepting these construction and shaping changes.

Do not subtract its phase sum from the latency median.

* Run the SQL benchmark

Build the release benchmark binary.

#+begin_src sh
cargo build --release --bin orgfdb-query-sql-benchmark
#+end_src

The SQL benchmark reuses compatible corpus databases when they exist.

It creates the work directory and missing corpus databases when required.

The first run can spend significant time preparing the ~50'000~-result database.

Use a persistent work directory when repeated benchmark runs need database reuse.

#+begin_src sh
target/release/orgfdb-query-sql-benchmark \
  --rows 100,1000,10000,50000 \
  --warmups 1 \
  --iterations 3 \
  --work-dir /tmp/orgfdb-presentation-benchmark-work \
  --output /tmp/orgfdb-query-sql-benchmark-rust-vs-sqlite-v1.json
#+end_src

Use a new output path for each benchmark run.

For the heading path variable-limit sensitivity task, use the existing ~50'000~-result database and run:

#+begin_src sh
target/release/orgfdb-query-sql-benchmark \
  --rows 50000 \
  --path-variable-limits 250000,32766,999 \
  --warmups 1 \
  --iterations 3 \
  --work-dir /tmp/orgfdb-presentation-benchmark-work \
  --output /tmp/orgfdb-query-sql-benchmark-path-variable-limits-v1.json
#+end_src

This focused run still records the normal ~50'000~-result benchmark sections.

The ~path_strategies~ section is the decision input for the variable-limit sensitivity task.

For the production heading path rebenchmark, run the standard result sizes without an explicit path variable limit.

#+begin_src sh
target/release/orgfdb-query-sql-benchmark \
  --rows 100,1000,10000,50000 \
  --warmups 1 \
  --iterations 3 \
  --work-dir /tmp/orgfdb-presentation-benchmark-work \
  --output /tmp/orgfdb-query-sql-benchmark-production-path-v1.json
#+end_src

Use the ~headings.path~ entry under ~workloads~ for the end-to-end comparison.

Use its ~direct_phase_profile~ for attribution.

For the metadata predicate benchmark, run the standard result sizes.

#+begin_src sh
target/release/orgfdb-query-sql-benchmark \
  --rows 100,1000,10000,50000 \
  --warmups 1 \
  --iterations 3 \
  --work-dir /tmp/orgfdb-presentation-benchmark-work \
  --output /tmp/orgfdb-query-sql-benchmark-predicate-driven-v1.json
#+end_src

Use ~metadata_predicate_strategies~ for the SQL-shape comparison.

Use ~compiler_audit~ for the impossible-lookup audit.

For the selective matched-relation refresh, run the standard result sizes with schema version ~10~.

#+begin_src sh
target/release/orgfdb-query-sql-benchmark \
  --rows 100,1000,10000,50000 \
  --warmups 1 \
  --iterations 3 \
  --work-dir /tmp/orgfdb-presentation-benchmark-work \
  --output /tmp/orgfdb-query-sql-benchmark-relation-reuse-v10.json
#+end_src

Use ~relation_reuse_strategies~ to refresh the isolated simple/complex comparison through the current compiler.

Use ~production_relation_reuse_strategies~ for the complete production enabled/disabled comparison.

Require exact public result equality before using the timing data.

For the complete metadata production A/B, use the same standard result sizes.

#+begin_src sh
target/release/orgfdb-query-sql-benchmark \
  --rows 100,1000,10000,50000 \
  --warmups 1 \
  --iterations 3 \
  --work-dir /tmp/orgfdb-presentation-benchmark-work \
  --output /tmp/orgfdb-query-sql-benchmark-predicate-driven-e2e-v1.json
#+end_src

Schema version ~7~ records ~production_metadata_predicate_strategies~ for ~10'000~ and ~50'000~ headings.

Use that section as the production latency decision input.

Schema version ~8~ adds ~metadata_index_costs~ at ~50'000~ headings.

For the index-cost gate, run only the largest result size:

#+begin_src sh
target/release/orgfdb-query-sql-benchmark \
  --rows 50000 \
  --warmups 1 \
  --iterations 3 \
  --work-dir /tmp/orgfdb-presentation-benchmark-work \
  --output /tmp/orgfdb-query-sql-benchmark-predicate-index-cost-v1.json
#+end_src

The cost section records logical database size, index build time, and one representative ~1'000~-row metadata write.

The local schema version ~8~ cost run accepts the three persistent candidates.

At ~50'000~ headings, the two property indexes add ~19.23 MiB~, or approximately ~11.45%~.

Their median build time is approximately ~603.75 ms~.

Their representative ~1'000~-row write increases from ~6.169 ms~ to ~7.759 ms~.

The absolute median write increase is approximately ~1.589 ms~.

The keyword index adds ~7.75 MiB~, or approximately ~4.61%~.

Its median build time is approximately ~688.61 ms~.

The representative keyword write shows no measured regression in this run.

All three indexes together add ~26.98 MiB~, or approximately ~16.07%~.

Their combined median build time is approximately ~1.157 s~.

The query latency gains justify these measured storage and maintenance costs.


* SQLite runtime identity

Query SQL benchmark schema version ~13~ records the effective SQLite runtime in the top-level environment.

The environment records:

- ~sqlite_runtime_kind~
- ~sqlite_runtime_label~
- ~sqlite_version~
- ~sqlite_source_id~
- ~sqlite_compile_options~
- ~sqlite_json_available~
- ~sqlite_fts5_available~
- ~sqlite_dbstat_available~
- ~sqlite_variable_limit~

The runtime values come from the linked SQLite library.

Treat ~sqlite_version~ and ~sqlite_source_id~ as the authoritative runtime identity.

Treat ~sqlite_runtime_kind~ and ~sqlite_runtime_label~ as descriptive build metadata.

This distinction prevents a build label from hiding an unexpected linked library.

** Required SQLite capabilities

Current query execution uses SQLite JSON SQL functions, including ~json_each~.

Search uses FTS5 when ~[search].fts5_enabled~ is true.

This option is enabled by default.

The general performance benchmark can use the DBSTAT virtual table for storage measurements.

The benchmark records probes for JSON, FTS5, and DBSTAT.

The Rust ~orgfdb_regexp~ function is registered through ~rusqlite~.

It does not require a SQLite regexp extension.

The benchmark also records the SQLite variable-number limit.

This limit affects ID chunk capacity in query execution.

** Bundled runtime direction

The implementation now selects ~rusqlite 0.40.2~ with ~libsqlite3-sys 0.38.2~.

The ~rusqlite~ dependency disables default features.

It enables only ~bundled~, ~functions~, ~limits~, and ~trace~.

This keeps the existing orgfdb feature set and avoids unrelated default dependencies.

The stable ~libsqlite3-sys 0.38.2~ bundle contains SQLite ~3.53.2~.

SQLite ~3.53.4~ is newer upstream, but this task does not maintain a custom SQLite source.

The actual ~sqlite_version~ and ~sqlite_source_id~ remain authoritative for each benchmark run.

The normal benchmark labels now default to ~bundled~ and ~rusqlite-bundled~.

The benchmark profiler now uses ~trace_v2~ with only ~SQLITE_TRACE_PROFILE~ events.

This replaces the deprecated ~rusqlite~ ~profile~ callback without changing measured query behavior.

The bundled build now has a schema version ~13~ runtime and performance baseline.

** Bundled schema version 13 baseline

The bundled benchmark reports SQLite ~3.53.2~ with source ID ~2026-06-03 19:12:13 d6e03d8c777cfa2d35e3b60d8ec3e0187f3e9f99d8e2ee9cac695fd6fcdf1a24~.

The runtime probes report:

- runtime kind ~bundled~
- runtime label ~rusqlite-bundled~
- JSON available
- FTS5 available
- DBSTAT available
- variable-number limit ~32766~

The previous system runtime used SQLite ~3.45.1~ with a variable-number limit of ~250000~.

The bundled runtime adds compile options that include ~DEFAULT_FOREIGN_KEYS~, ~ENABLE_API_ARMOR~, ~ENABLE_MEMORY_MANAGEMENT~, and ~ENABLE_STAT4~.

The system runtime had extra optional features that orgfdb does not require, including FTS4, math functions, session support, and statement virtual tables.

The required JSON, FTS5, and DBSTAT capabilities are present in the bundled runtime.

The standard schema version ~13~ complete-query medians compare as follows.

| Results | Workload | System SQLite 3.45.1 | Bundled SQLite 3.53.2 | Change |
|---------+----------+----------------------+-----------------------+--------|
| 100 | headings.normal | 0.623 ms | 0.463 ms | -25.7% |
| 100 | headings.tags | 0.411 ms | 0.567 ms | +38.0% |
| 100 | headings.properties | 0.694 ms | 0.912 ms | +31.4% |
| 100 | headings.effective-properties | 0.847 ms | 0.716 ms | -15.5% |
| 100 | headings.path | 0.774 ms | 0.857 ms | +10.7% |
| 100 | files.normal | 0.302 ms | 0.536 ms | +77.8% |
| 100 | files.keywords | 0.393 ms | 0.418 ms | +6.5% |
| 1000 | headings.normal | 3.093 ms | 4.894 ms | +58.2% |
| 1000 | headings.tags | 3.860 ms | 4.241 ms | +9.9% |
| 1000 | headings.properties | 5.114 ms | 7.347 ms | +43.7% |
| 1000 | headings.effective-properties | 5.991 ms | 4.911 ms | -18.0% |
| 1000 | headings.path | 5.276 ms | 6.148 ms | +16.5% |
| 1000 | files.normal | 2.065 ms | 3.866 ms | +87.2% |
| 1000 | files.keywords | 3.264 ms | 4.485 ms | +37.4% |
| 10000 | headings.normal | 45.673 ms | 34.969 ms | -23.4% |
| 10000 | headings.tags | 58.629 ms | 38.858 ms | -33.7% |
| 10000 | headings.properties | 102.200 ms | 70.709 ms | -30.8% |
| 10000 | headings.effective-properties | 66.456 ms | 49.428 ms | -25.6% |
| 10000 | headings.path | 71.296 ms | 66.103 ms | -7.3% |
| 10000 | files.normal | 28.731 ms | 20.888 ms | -27.3% |
| 10000 | files.keywords | 48.901 ms | 39.057 ms | -20.1% |
| 50000 | headings.normal | 209.481 ms | 189.553 ms | -9.5% |
| 50000 | headings.tags | 244.509 ms | 188.439 ms | -22.9% |
| 50000 | headings.properties | 408.382 ms | 298.967 ms | -26.8% |
| 50000 | headings.effective-properties | 314.415 ms | 273.617 ms | -13.0% |
| 50000 | headings.path | 411.178 ms | 390.317 ms | -5.1% |
| 50000 | files.normal | 120.974 ms | 134.772 ms | +11.4% |
| 50000 | files.keywords | 263.100 ms | 251.132 ms | -4.5% |

The small-result comparisons are mixed, but the largest absolute increase at 1000 results is about ~2.23 ms~.

All seven 10000-result workloads improve in this run.

Six of seven 50000-result workloads improve.

The 50000-result ~files.normal~ workload is the only large-workload regression in the primary complete-query samples.

Its median increases from ~120.974 ms~ to ~134.772 ms~, or about ~11.4%~.

The separate direct-phase profile does not show the same regression.

For ~files.normal~, bundled SQLite reduces matched SQL execution, enrichment SQL execution, row decoding, and final result shaping in that profile.

This mismatch is consistent with run-level variance outside the isolated attribution sample.

Run one focused 50000-result repeat with more timing samples before treating the ~files.normal~ increase as a bundled-runtime regression.

The lower variable-number limit is handled correctly by the existing query chunking.

For the 50000-result ~headings.path~ workload, the direct profile increases from four statements with a maximum of 50000 bound parameters to six statements with a maximum of 32766.

The complete-query median still improves by about ~5.1%~.

Four of the 13 representative query plans change under SQLite ~3.53.2~.

The changed plans are ~hierarchy-parent~, ~link-source~, ~link-target~, and ~file-restriction~.

SQLite ~3.53.2~ uses explicit ~SEARCH ... EXISTS~ plan entries where SQLite ~3.45.1~ reported correlated scalar subqueries.

The other nine recorded plans stay unchanged.

** Invalid controlled-runtime attempt

The first schema version ~13~ runtime comparison exposed a build error in the comparison procedure.

The file labeled ~system-default~ reported SQLite ~3.45.1~.

The file labeled ~sqlite-3.53.4-amalgamation~ also reported SQLite ~3.45.1~.

Both files reported the same ~sqlite_source_id~ and compile options.

The second binary therefore did not load SQLite ~3.53.4~.

Do not use those timing differences as SQLite version evidence.

This result confirms why runtime identity belongs in the benchmark output.

The external shared-library comparison procedure is no longer part of the project plan.

The bundled schema version ~13~ baseline above replaces this invalid comparison.

* Heading-tag SQLite aggregation investigation

A temporary schema version ~14~ benchmark tested SQLite-side aggregation for heading tags.

The production baseline returned one row per effective tag and grouped tags in Rust.

The candidate used ~json_group_array(tag ORDER BY position)~ and returned one aggregate row per heading.

The benchmark required exact public ~QueryResponse~ equality before it collected timing samples.

The bundled SQLite runtime was ~3.53.2~ with ~MAX_VARIABLE_NUMBER=32766~.

The paired complete-query medians are:

| Results | Row-per-tag | JSON aggregate | Change |
|---------+-------------+----------------+--------|
| 100 | 0.715 ms | 0.757 ms | +6.0% |
| 1'000 | 4.532 ms | 4.616 ms | +1.8% |
| 50'000 | 261.575 ms | 261.046 ms | -0.2% |

At ~50'000~ results, tag rows fall from ~150'000~ to ~50'000~.

Total SQLite-to-Rust rows fall from ~250'000~ to ~150'000~.

The tag payload grows from ~950 KB~ to ~1.45 MB~ because the aggregate carries JSON syntax.

The ~50'000~-result tag phase explains the neutral complete-query result:

| Phase | Row-per-tag | JSON aggregate |
|-------+-------------+----------------|
| SQLite execution | 45.574 ms | 78.150 ms |
| SQLite row decoding | 26.893 ms | 8.186 ms |
| Rust grouping | 10.469 ms | 8.483 ms |
| Rust local sorting | 11.251 ms | 0 ms |
| Total | 94.187 ms | 94.818 ms |

SQLite aggregation reduces boundary rows and Rust work, but the aggregate SQL costs more.

The end-to-end gain at ~50'000~ results is only about ~0.2%~.

The candidate is slower at ~100~ and ~1'000~ results.

Reject the candidate and keep the existing row-per-tag production strategy.

Do not generalize the pattern to properties, effective properties, or file keywords from this result.

The temporary schema version ~14~ strategy harness was removed after the decision.

The retained query SQL benchmark output remains schema version ~13~.

* Benchmark output

Each result size records the runtime SQLite variable limit and resulting ID chunk capacity.

Each production workload records:

- result count
- total query timing distribution
- actual SQL statement count from one profile run
- total bound parameters
- maximum bound parameters in one statement
- SQLite profile duration by stage
- one direct phase profile
- rows transferred from SQLite during the direct profile

The lookup section records ID transport strategy timings.

The relation-reuse section records simple and complex relation timings for single and multi enrichment.

The path-strategy section compares the recursive query-derived loader with the Rust-driven bulk ancestor loader.

Schema version ~4~ also records the requested and effective SQLite variable limit plus ID chunk capacity for every path strategy result.

Schema version ~5~ adds a complete production path A/B after match and final shaping costs.

Schema version ~6~ adds metadata predicate SQL-shape measurements at four selectivities.

Schema version ~7~ adds complete production metadata A/B measurements for the primary large result sizes.

Schema version ~8~ adds persistent metadata index cost measurements for the ~50'000~-heading corpus.

Schema version ~9~ keeps the pre-change metadata A/B reproducible after the production indexes exist.

Its benchmark copies remove the production predicate indexes before they recreate the legacy and indexed comparison.

Schema version ~9~ also records complete metadata production A/B results for small result sizes.

Schema version ~10~ refreshes relation-reuse workloads through the current production compiler, includes the complete TEMP lifecycle in the isolated comparison, and adds complete production A/B results for selective matched-relation reuse.

Schema version ~11~ adds measurement-only direct-flat final shaping detail for the remaining Rust result-construction investigation.

Schema version ~12~ adds a paired in-process production A/B between the pre-candidate clone behavior and the ownership-transfer candidate.

Schema version ~13~ adds SQLite runtime identity, compile options, capability probes, the variable limit, and descriptive build labels.

The top-level ~compiler_audit~ records safe related-lookup findings.

The top-level query-plan section records representative compiled queries and their SQLite plan details.

The broad production workload set contains:

- ~headings.normal~
- ~headings.tags~
- ~headings.properties~
- ~headings.effective-properties~
- ~headings.path~
- ~files.normal~
- ~files.keywords~

* Current design decision

Production now keeps query-derived relations for statically cheap heading matches and selects one shared per-query TEMP matched relation for statically expensive heading matches.

The relation-reuse benchmark does not support one global TEMP strategy.

Simple matched relations favor repeated query-derived joins, including measured workloads with several enrichment consumers.

Complex reused relations favor one shared TEMP relation, including the measured single-enrichment workload.

The current static rule therefore classifies repeated relational work from the predicate tree rather than result count or consumer count.

The supplied schema version ~10~ local benchmark validates the selective strategy against the current predicate-driven compiler.

The metadata predicate benchmark favors relation-driven SQL for all four measured predicate families.

The complete production A/B confirms large selective-query gains at ~10'000~ and ~50'000~ headings.

At ~50'000~ headings and ~1%~ selectivity, the predicate-driven candidates reduce complete query latency by approximately ~95-96%~ with the required indexes.

The indexed direct-property, effective-property, and keyword candidates remain faster at ~100%~ selectivity.

Their ~50'000~-heading gains at ~100%~ are approximately ~10.0%~, ~7.9%~, and ~7.4%~.

Direct tags use the existing ~idx_tags_tag~ index and need no additional tag index.

The direct-tag candidate is approximately ~2.1%~ slower at ~50'000~ headings and ~100%~ selectivity.

Do not treat predicate-driven direct-tag matching as an unconditional win for every selectivity.

The schema version ~8~ cost gate accepts both property indexes and the keyword index.

Production schema version ~13~ adds these indexes:

- ~idx_effective_properties_key_local_heading~
- ~idx_effective_properties_key_effective_heading~
- ~idx_keywords_keyword_value_heading~

Production heading queries use predicate-driven ~IN (SELECT ...)~ for non-regexp direct tags, properties, effective properties, and keywords.

Direct tags reuse ~idx_tags_tag~ and add no second tag index.

Regexp metadata predicates keep the heading-driven ~EXISTS~ form.

The final schema version ~9~ run measures the ~50'000~-heading direct-tag ~100%~ case at approximately ~2.62%~ slower.

The retained strategy accepts this narrow nonselective regression because selective direct-tag queries improve much more.

The final small-result run covers ~100~, ~1'000~, and ~10'000~ eligible headings.

At ~100~ headings, direct tag at ~100%~ selectivity changes from ~0.803 ms~ to ~0.809 ms~, or approximately ~0.68%~ slower.

All other retained ~100~-heading cases improve.

At ~1'000~ and ~10'000~ headings, every retained candidate improves at every measured selectivity.

The small-result evidence therefore shows no material regression.

The implementation keeps runtime-limit ~IN (...)~ as the safe fallback for Rust-originated ID sets.

The production direct-flat heading path now uses the Rust-driven bounded ancestor loader.

Schema version ~3~ showed that this loader wins at the local ~250000~ variable limit.

Schema version ~4~ confirmed the advantage at ~250000~, ~32766~, and ~999~.

The recursive query-derived loader remains only as a benchmark reference.

The production rebenchmark shows clear gains through ~10'000~ results and a ~13.7%~ regression at ~50'000~ results.

The same-process loader comparison still favors ~rust-driven-bulk-ancestors~ by approximately ~25.0%~ at ~50'000~ results.

Keep the current Rust-driven loader while schema version ~5~ measures the complete same-process production A/B.

Accept the construction and shaping optimization only if the complete production benchmark improves.
