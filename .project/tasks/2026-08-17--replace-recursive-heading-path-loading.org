#+TITLE: Replace Recursive Heading Path Loading with Rust-Driven Bulk Ancestors
#+STARTUP: showall
#+TEMPLATE_VERSION: 0.8.0
#+TASK_STARTED: [2026-08-17 Monday 14:15]
#+TASK_COMPLETED:

* Original Task (User only)
:PROPERTIES:
:VISIBILITY: folded
:END:

Continue ~Replace recursive heading path loading with Rust-driven bulk ancestors~ from ~todo.org~.

* Planning

** Problem summary

The production direct-flat heading path loader still uses the recursive query-derived SQL strategy.

The benchmarked Rust-driven bulk ancestor strategy is faster at all tested result sizes.

The variable-limit benchmark also keeps the Rust-driven strategy faster at ~250000~, ~32766~, and ~999~.

** Goals

- Use the Rust-driven bulk ancestor loader for production direct-flat heading path enrichment.
- Reuse matched heading rows that are already in Rust.
- Load only missing ancestors with runtime-limit-safe ID batches.
- Preserve current path output and corruption errors.
- Keep the recursive strategy as the benchmark reference for the next rebenchmark task.

** Non-goals

- Do not change query matching or relational filtering.
- Do not change public query output.
- Do not remove the recursive benchmark strategy yet.
- Do not change the benchmark protocol in this task.
- Do not add dependencies.

** Architecture

Keep matched query relations in SQLite for filtering and enrichment selection.

For direct-flat ~path~ enrichment, reuse matched heading rows in Rust.

Collect missing parent IDs and load only those headings in bounded ID batches.

Validate ~outline_path~ rows for matched headings and loaded ancestors.

Build the final file and heading path in Rust.

** Execution plan

1. Switch production direct-flat heading path enrichment to the Rust-driven loader.
2. Keep the recursive loader available only for benchmark comparison and regression checks.
3. Rename benchmark-only SQL trace labels that still describe the Rust loader as benchmark-only.
4. Add production regression tests for missing ancestors and malformed ~outline_path~ JSON.
5. Keep existing nested path, small variable limit, cross-file parent, and missing ~outline_path~ tests.
6. Update query SQL performance documentation with the production strategy decision.
7. Run ~make ci~.
8. Record results and set the task to ~REVIEW~.

** Risks & assumptions

- The Rust-driven loader already has output-equality coverage against the recursive strategy.
- Small SQLite variable limits can increase statement count, but the loader remains runtime-limit safe.
- The next task measures final production end-to-end performance after this change.

** Open questions

None.

** Proposed checks

- ~cargo fmt --all --check~
- ~cargo clippy --all-targets --all-features -- -D warnings~
- ~cargo test --all-targets --all-features~
- ~make ci~

* Results

** Approach

The production direct-flat heading path loader now uses the Rust-driven bulk ancestor strategy.

The old recursive query-derived loader remains available as the benchmark reference.

The production loader reuses matched heading rows, collects missing parent IDs, and loads only required ancestors.

Ancestor loads use the runtime SQLite variable limit for every ID batch.

The loader keeps current ~outline_path~, missing-row, cross-file parent, and malformed-JSON errors.

Benchmark-only SQL marker names on the Rust-driven loader now use production enrichment names.

** Changed files

- ~src/query/result.rs~
- ~src/query_sql_benchmark.rs~
- ~docs/query-sql-performance.org~
- ~.project/tasks/todo.org~
- this task file

** Checks performed

~make ci~ was attempted.

A source diff audit confirmed that only the five task files changed.

A symbol audit confirmed that no ~load_heading_paths_rust_driven~ references remain.

A SQL marker audit confirmed that the Rust-driven production queries use ~enrich-path-rust-*~ markers.

** Test results

Existing tests cover nested path equality, small SQLite variable limits, cross-file parents, and missing ancestor ~outline_path~ rows.

New production regression tests cover a missing ancestor and malformed ancestor ~breadcrumbs_json~.

The Rust test suite needs the local Rust environment.

** Blockers

This execution environment does not provide ~cargo~.

~make ci~ stops at ~cargo fmt --all --check~ before Rust checks can run.

The supplied archive has no Git metadata, so this environment cannot create the task branch.

** Follow-ups

Run ~make ci~ locally.

Then continue with ~Rebenchmark production heading path after Rust ancestor loading~.
