#+TITLE: Materialize effective properties in schema version 10
#+TASK_STARTED: [2026-07-25 Saturday 12:00]

* Planning

- Materialize the existing property-resolution contract from canonical rows and the stored heading tree.
- Add schema version 10, transactional backfill, query projection use, and documentation.

* Build

- Added the normalized ~effective_properties~ table with local and inherited values.
- Kept ~properties~ canonical and reused one shared root-to-leaf derivation for normal indexing, test fixtures, and migration backfill.
- Moved property-key normalization into the neutral shared property module.
- Made migration backfill file-scoped and validated roots, parent ownership, reachability, and cycles before inserting derived rows.
- Switched property predicates and included effective-property output to the stored projection.
- Removed the obsolete recursive query-time resolver and legacy SQL.

* Checks

- The implementation is ready for ~make ci~ in the user environment.
- The review environment did not provide ~cargo~, ~rustfmt~, or ~rustc~, so Rust checks were not run here.
- The existing benchmark runner was intentionally left unchanged; performance verification remains a separate run after CI.
