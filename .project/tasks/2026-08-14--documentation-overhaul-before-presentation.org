#+TITLE: Documentation Overhaul Before Presentation Work
#+STARTUP: showall
#+TEMPLATE_VERSION: 0.8.0
#+TASK_STARTED: [2026-08-14 Fri 22:35]
#+TASK_COMPLETED:

* Original Task (User only)
:PROPERTIES:
:VISIBILITY: folded
:END:

Overhaul the ~org-files-db~ documentation before presentation work. Use the current implementation as the source of truth. Keep the README user-facing, separate machine-readable contracts and contributor architecture, and document the planned presentation architecture as planned work. Use clear Simple English throughout the documentation.

** DONE Overhaul the org-files-db documentation before presentation work
:PROPERTIES:
:VISIBILITY: folded
:END:

Task file: [[./2026-08-14--documentation-overhaul-before-presentation.org][Documentation Overhaul Before Presentation Work]]

Goal: Reorganize and rewrite the project documentation so that the current public behaviour, internal architecture, and planned presentation interface are clearly separated before implementing the next architecture.

Treat the existing implementation as the source of truth for current behaviour.

Do not document planned functionality as already implemented.

*** Documentation structure

Separate documentation by audience and purpose.

At minimum distinguish:

- user-facing CLI documentation
- query language and query result documentation
- configuration documentation
- architecture / contributor documentation
- stable machine-readable output contracts
- planned presentation architecture

Keep the main README concise and user-facing.

Move implementation details and architectural explanations into dedicated documentation where appropriate.

*** Current CLI behaviour

Document the current supported commands and options from the implementation.

In particular document:

- configuration handling
- indexing
- querying
- query result kinds
- query includes
- JSON output
- changes / generations where relevant
- file restrictions where relevant
- mutation commands where relevant

Do not infer options from old documentation when they no longer exist.

*** Existing JSON contract

Document the existing:

#+begin_src sh
orgfdb query --format json ...
#+end_src

contract as the stable compatibility interface.

Make clear that future presentation output is a separate opt-in representation and does not replace or modify the existing JSON contract.

Document result kinds and optional included data sufficiently for client implementations such as ~org-files-db.el~.

*** Query documentation

Document the supported structural query language from the current parser and implementation.

Use practical examples such as:

#+begin_src sh
orgfdb query '(headings)'
orgfdb query '(headings (not (done)))'
orgfdb query '(links (status "resolved"))'
orgfdb query '(files (tags "project"))'
#+end_src

Avoid duplicating implementation internals unless they are required to understand public behaviour.

*** Architecture documentation

Document the current high-level architecture, including where useful:

- discovery
- parsing
- indexing
- database generations
- query execution
- result construction
- includes
- changes
- mutations
- output serialization

Keep implementation documentation separate from the user README.

*** Planned presentation design

Add a design document for the planned presentation architecture.

Clearly mark it as planned until implemented.

Capture the architectural decisions already made:

- existing ~query --format json~ remains unchanged
- presentation is opt-in
- presentation belongs to ~orgfdb query~
- one structured ~PresentationSpec~
- Rust performs UI-independent presentation work
- Emacs maps semantic roles to concrete faces
- results, rows, and cells are separate concepts
- rows reference original results instead of duplicating them
- full search values remain separate from visible truncated values
- no search support in this architecture
- no Child Emacs / ~async.el~
- no presentation cache until benchmarks justify one

Include the intended pipeline:

#+begin_example
query
  -> results
  -> row expansion
  -> cell extraction
  -> sort
  -> shared widths
  -> truncation / padding
  -> presentation rows
#+end_example

Do not prematurely freeze incidental implementation details that have not yet been tested.

*** Stale documentation

Audit existing documentation for:

- removed commands
- renamed options
- obsolete cache designs
- old query-model terminology
- stale JSON examples
- implementation details presented as public API

Remove or update stale material.

*** Documentation maintenance

Establish clear locations for future updates so presentation work does not require duplicating the same documentation in several files.

Acceptance criteria:

- The README describes the current CLI from a user perspective.
- Current query syntax and result behaviour are documented.
- The existing JSON contract is explicitly identified as stable.
- Architecture documentation is separate from normal user documentation.
- The planned presentation architecture has one dedicated design document.
- Planned features are clearly distinguished from implemented features.
- Obsolete cache and Child-Emacs assumptions are removed from current architecture documentation.
- Documentation examples match the current executable.
- Future presentation tasks have an obvious documentation location to update.

* Planning

** Problem summary

The current README mixes user guidance, implementation notes, schema details, link-resolution internals, and cache architecture. The CLI reference is large, and the query reference uses old ~Query Model v0~ terminology in user-facing sections. The documentation needs clearer ownership and audience boundaries before the presentation interface is implemented.

** Goals

- Make ~README.org~ a concise user entry point.
- Keep the current CLI behavior and configuration reference accurate.
- Give the stable ~query --format json~ contract a clear reference location.
- Keep detailed query-language semantics in a dedicated reference.
- Add a current architecture document for contributors.
- Add a planned presentation design document that does not describe planned work as implemented.
- Use clear, consistent technical English.

** Non-goals

- Do not change Rust behavior.
- Do not implement ~presentation-json~.
- Do not change the existing JSON contract.
- Do not redesign the query language.
- Do not change configuration keys or CLI options.

** Architecture

Documentation ownership will be split by audience:

- ~README.org~: installation, first use, core commands, and links to detailed references.
- ~docs/cli.org~: command and option reference.
- ~docs/config.org~: configuration reference.
- ~docs/reference/query-language.org~: structural query language.
- ~docs/reference/query-json.org~: stable machine-readable query JSON contract.
- ~docs/architecture.org~: current implementation architecture.
- ~docs/design/presentation.org~: planned presentation architecture.
- existing focused design documents: detailed indexing, generation, and parser topics.

** Execution plan

1. Compare the current documentation with the CLI, configuration, query parser, validator, and result-shaping code.
2. Rewrite the README as a short user entry point.
3. Refocus the CLI and configuration references on current public behavior.
4. Rename and edit the query reference so user-facing documentation does not use versioned model terminology.
5. Add a separate query JSON contract reference.
6. Add a current architecture overview.
7. Add the planned presentation design document.
8. Repair links and remove stale or duplicated claims.
9. Run documentation checks and ~make ci~.

** Risks & assumptions

- The archive has no Git metadata, so no task branch can be created in this working copy.
- The implementation is authoritative when documentation and code disagree.
- Historical design documents remain available when they still describe useful internal contracts.

** Open questions

None. The approved task gives enough scope for implementation.

** Proposed checks

- Search documentation for stale ~Query Model v0~ wording outside historical contexts.
- Check Org file links after file moves or renames.
- Check documented CLI options against Clap definitions.
- Check documented configuration keys against deserialization structs.
- Run the Simple English final audit on changed prose.
- Run ~make ci~.

* Build

- Reorganized the documentation by audience and contract.
- Rewrote the README, CLI reference, configuration reference, and contributor guides in clear technical English.
- Replaced the versioned query-model document with a current structural query reference.
- Added a separate stable query JSON contract.
- Added a current architecture overview and a planned presentation design.
- Updated stale parser and cache-oriented contributor documentation to match the current implementation.
- Updated the query help text to remove the old model-version term.

* Results

** Approach

Used the current Clap, configuration, query validator, result structs, parser, and indexer code as the source of truth. Split user guidance, machine contracts, current architecture, and planned design into separate documents. Applied the Simple English documentation rules in pragmatic mode.

** Changed files

- Rewrote ~README.org~ and ~CONTRIBUTING.org~.
- Added ~docs/README.org~ and ~docs/architecture.org~.
- Rewrote ~docs/cli.org~ and ~docs/config.org~.
- Replaced ~docs/reference/query-model-v0.org~ with ~docs/reference/query-language.org~.
- Added ~docs/reference/query-json.org~.
- Rewrote ~docs/design/incremental-indexing.org~ and ~docs/design/parser-risks.org~.
- Replaced ~docs/design/index-generations-and-cached-views.org~ with ~docs/design/index-generations-and-changes.org~.
- Added ~docs/design/presentation.org~ as planned architecture.
- Rewrote ~docs/performance-baseline.org~ for clearer technical English.
- Updated the query argument help text in ~src/cli.rs~.
- Updated this task file and ~.project/tasks/todo.org~.

** Checks performed

- Compared documented commands with the Clap ~Command~ enum.
- Compared documented top-level configuration keys with ~RawConfig~.
- Compared query predicates with the validator dispatch.
- Compared query includes with ~QueryInclude~.
- Compared query JSON fields with the public result structs.
- Checked local documentation links after file renames.
- Checked Org source-block balance in the rewritten documentation.
- Searched for stale renamed documentation paths.
- Ran the Simple English final audit across ~README.org~, ~CONTRIBUTING.org~, and all files under ~docs/~.
- Ran ~make ci~.

** Test results

The static documentation and source-consistency checks passed. The documentation link and Simple English checks also passed.

~make ci~ stopped at its first command because ~cargo~ is not installed in this container:

#+begin_example
cargo fmt --all --check
make: cargo: No such file or directory
make: *** [Makefile:19: fmt] Error 127
#+end_example

** Blockers

The current container has no Cargo toolchain, so the repository CI checks cannot run here. The uploaded archive also has no Git metadata, so no task branch can be created.

** Follow-ups

The historical benchmark identifier ~query-model-v0~ remains unchanged in benchmark code because it identifies recorded measurement data. The next architecture task can now use ~docs/design/presentation.org~ as its design reference.
