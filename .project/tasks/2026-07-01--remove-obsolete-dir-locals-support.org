#+TITLE: Remove obsolete .dir-locals.el support
#+STARTUP: showall
#+TEMPLATE_VERSION: 0.7.0
#+TASK_STARTED: [2026-07-01 Wed 14:45]
#+TASK_COMPLETED: [2026-07-01 Wed 14:51]

* Input & Todo Task
:PROPERTIES:
:VISIBILITY: folded
:END:

Copied task entry:

#+begin_quote
** BUILD Remove obsolete .dir-locals.el support
:PROPERTIES:
:RISK: Medium
:DB_SCHEMA_CHANGE: No
:API_CONTRACT_CHANGE: Yes
:END:

Goal:

- Remove all obsolete ~.dir-locals.el~ support from the codebase.
- Remove the ~parse.dir_locals~ config surface.
- Remove the dedicated ~dir_locals~ parser/resolver module.
- Keep the project policy simple: ~.dir-locals.el~ is not supported and does not participate in indexing.

Repository findings:

- ~.dir-locals.el~ support still appears in:
  - ~docs/config.md~
  - ~docs/parser-risks.org~
  - ~README.org~
  - ~src/config.rs~
  - ~src/dir_locals.rs~
  - ~src/indexer.rs~
  - ~src/lib.rs~
  - ~src/db.rs~
  - ~src/db/schema.rs~
  - project planning/task notes
- The main obsolete implementation appears to be:
  - ~src/dir_locals.rs~
  - ~pub mod dir_locals;~ in ~src/lib.rs~
  - ~DirLocalsConfig~
  - ~DirLocalsUnsupportedPolicy~
  - ~RawDirLocalsConfig~
  - ~parse.dir_locals~ config parsing and tests
  - indexer tests that create or configure ~.dir-locals.el~
- There is also legacy migration/test code around removing ~dir_locals~ as a TODO keyword source kind.

Decision:

- Remove ~.dir-locals.el~ as a supported feature.
- Do not keep a disabled config option.
- Do not keep a resolver that always returns empty results.
- Do not keep ~parse.dir_locals~ in documentation.
- Treat any remaining mention only as historical task/archive material unless it appears in active code, docs, README, or current config reference.

Current obsolete config model:

#+begin_src toml
,[parse.dir_locals]
enabled = false
inherit = true
unsupported = "warn"
#+end_src

New config contract:

- No ~parse.dir_locals~ table.
- No ~parse.dir_locals.enabled~.
- No ~parse.dir_locals.inherit~.
- No ~parse.dir_locals.unsupported~.
- No top-level or nested config for ~.dir-locals.el~ handling.

Contract:

- The indexer does not read ~.dir-locals.el~.
- The parser does not parse ~.dir-locals.el~.
- TODO keyword resolution uses supported project sources only:
  - in-buffer Org keyword declarations
  - config defaults
- ~.dir-locals.el~ does not override TODO keywords.
- ~.dir-locals.el~ does not emit diagnostics.
- Unsafe Emacs Lisp is not evaluated because ~.dir-locals.el~ files are not read at all.
- Config loading rejects obsolete ~parse.dir_locals~ keys with a clear error if unknown-field rejection is already part of the config parser.
- If unknown fields are currently allowed, remove documentation and code support first; add strict rejection only if it matches existing config behavior.

Scope:

- Remove ~src/dir_locals.rs~.
- Remove ~pub mod dir_locals;~ from ~src/lib.rs~.
- Remove ~DirLocalsConfig~ from the public/internal config model.
- Remove ~DirLocalsUnsupportedPolicy~.
- Remove ~RawDirLocalsConfig~.
- Remove ~parse.dir_locals~ deserialization.
- Remove defaults for ~parse.dir_locals~.
- Remove config tests for ~parse.dir_locals~.
- Remove indexer tests whose only purpose is proving ~.dir-locals.el~ behavior.
- Remove or rewrite docs that document ~parse.dir_locals~ as a supported config option.
- Update config reference so ~dir-locals~ no longer appears as an active supported feature.
- Keep any DB migration that is still needed to clean old databases, but remove misleading active-feature wording.
- Keep historical archived task notes unchanged unless they confuse active documentation.

Files to inspect and likely update:

- ~src/config.rs~
  - remove config structs/enums related to ~dir_locals~
  - remove raw config parsing for ~parse.dir_locals~
  - remove tests:
    - ~dir_locals_support_is_disabled_by_default~
    - ~parses_dir_locals_config_values~
    - ~rejects_invalid_dir_locals_unsupported_value~
  - remove imports of ~DirLocalsConfig~ and ~DirLocalsUnsupportedPolicy~

- ~src/dir_locals.rs~
  - delete file

- ~src/lib.rs~
  - remove ~pub mod dir_locals;~

- ~src/indexer.rs~
  - remove tests that create ~.dir-locals.el~ files or configure ~[parse.dir_locals]~
  - remove obsolete test names such as:
    - ~rebuild_ignores_dir_locals_when_disabled~
    - ~rebuild_ignores_dir_locals_todo_keywords_when_enabled~
    - ~rebuild_preserves_behavior_when_dir_locals_is_missing~
    - ~rebuild_ignores_dir_locals_even_when_inherit_is_disabled~
    - ~rebuild_ignores_deepest_matching_dir_locals_todo_keywords~
    - ~rebuild_ignores_dir_locals_for_explicit_files_inside_configured_dirs~
    - ~rebuild_emits_no_dir_locals_warning_for_unsafe_reader_syntax~
    - ~rebuild_uses_config_defaults_for_eval_only_dir_locals_without_warning~
    - ~rebuild_dir_locals_todo_keywords_do_not_override_config_defaults~
    - ~rebuild_org_keywords_override_config_defaults_even_when_dir_locals_exists~
  - keep or add a smaller regression test only if useful:
    - create a ~.dir-locals.el~ next to an Org file
    - do not configure any ~parse.dir_locals~ option
    - assert indexing behaves exactly as if the file did not exist

- ~src/db.rs~
  - inspect migration/test code around ~dir_locals~ source kind
  - keep migration coverage if old DB cleanup still matters
  - rename wording if needed so it is clearly legacy cleanup, not active support

- ~src/db/schema.rs~
  - inspect functions such as ~todo_keywords_table_supports_dir_locals~
  - keep only if needed for legacy migration
  - otherwise remove

- ~docs/config.md~
  - remove ~parse.dir_locals~ section completely
  - remove ~parse.dir_locals~ from supported top-level/nested keys
  - ensure config reference says no ~.dir-locals.el~ config exists

- ~docs/parser-risks.org~
  - remove or rewrite active statements about ~.dir-locals.el~ handling
  - keep only a short risk/policy note if still useful:
    - ~.dir-locals.el~ is intentionally outside the config and parser contract
    - the project does not read or evaluate it

- ~README.org~
  - remove active feature/security bullets that imply implemented ~.dir-locals.el~ handling
  - optionally keep a simple policy sentence:
    - ~.dir-locals.el~ is not supported by the indexer

- ~.project/tasks/roadmap.org~ and archived task files
  - do not spend time rewriting historical archived context unless it appears in current roadmap or active task lists
  - update active roadmap references if they imply future ~.dir-locals.el~ support

Non-goals:

- Do not add new ~.dir-locals.el~ support.
- Do not parse a safe subset of Emacs Lisp.
- Do not evaluate Emacs Lisp.
- Do not add diagnostics for ignored ~.dir-locals.el~ files.
- Do not add compatibility for ~parse.dir_locals~.
- Do not add a new config replacement.
- Do not change database schema unless required by existing migration cleanup.
- Do not rewrite historical archived task files unless they are surfaced as active documentation.

Acceptance criteria:

- [ ] ~src/dir_locals.rs~ is removed.
- [ ] ~src/lib.rs~ no longer exports ~dir_locals~.
- [ ] Config model no longer contains ~DirLocalsConfig~.
- [ ] Config model no longer contains ~DirLocalsUnsupportedPolicy~.
- [ ] Config parser no longer accepts or documents ~parse.dir_locals~ as supported behavior.
- [ ] Tests for parsing ~parse.dir_locals~ are removed.
- [ ] Indexer tests no longer configure ~[parse.dir_locals]~.
- [ ] Active documentation no longer documents ~parse.dir_locals~.
- [ ] Active documentation no longer presents ~.dir-locals.el~ as a supported feature.
- [ ] ~docs/config.md~ contains no supported ~dir_locals~ config section.
- [ ] README does not imply ~.dir-locals.el~ support.
- [ ] Any remaining ~dir_locals~ mention is either legacy migration cleanup or historical archived context.
- [ ] Existing TODO keyword behavior remains covered by in-buffer keyword and config-default tests.
- [ ] ~make ci~ passes.

Suggested tests:

- Config rejection test:
  - Use a config containing:
    #+begin_src toml
    ,[parse.dir_locals]
    enabled = true
    #+end_src
  - Assert config loading fails clearly if unknown config fields are rejected.
  - If unknown fields are not rejected globally, skip this test and do not special-case compatibility.

- Indexing policy regression test:
  - Create an Org file with normal TODO keywords.
  - Create a neighboring ~.dir-locals.el~ file that would define different TODO keywords in Emacs.
  - Run rebuild.
  - Assert the indexed TODO keywords come only from supported sources.
  - Assert no ~.dir-locals.el~ diagnostic or warning is emitted.

- Documentation check:
  - Search active docs for:
    - ~parse.dir_locals~
    - ~DirLocalsConfig~
    - ~DirLocalsUnsupportedPolicy~
  - Assert no active docs/config reference expose them as supported behavior.

Suggested commit split:

1. Remove ~dir_locals~ config model and parser support.
2. Remove ~src/dir_locals.rs~ and module export.
3. Remove or rewrite indexer/config tests.
4. Clean active documentation and README references.
5. Keep or clarify legacy DB migration cleanup if still needed.
#+end_quote

Chat context:

- Implement the task exactly as a BUILD task.
- When copying the todo entry from ~todo.org~ into the task file, copy the full entry instead of only a truncated heading.

* Results

** Approach

- Removed the obsolete ~parse.dir_locals~ config model and deleted the dedicated ~src/dir_locals.rs~ module instead of keeping a disabled compatibility layer.
- Replaced the old ~.dir-locals.el~-focused indexer tests with one smaller regression that proves neighboring ~.dir-locals.el~ files do not affect effective TODO handling.
- Cleaned the active user-facing documentation so it reflects the current CLI/config contract and the project policy that ~.dir-locals.el~ is not read.

** Changed files

- ~src/config.rs~
- ~src/indexer.rs~
- ~src/lib.rs~
- ~src/dir_locals.rs~ (deleted)
- ~docs/config.md~
- ~docs/cli.md~
- ~docs/parser-risks.org~
- ~README.org~
- ~.project/tasks/roadmap.org~

** Checks performed

- Searched active code and docs for ~parse.dir_locals~, ~DirLocalsConfig~, ~DirLocalsUnsupportedPolicy~, and ~.dir-locals.el~ support references.
- Ran ~cargo fmt --all~.
- Ran ~make ci~.

** Test results

- ~make ci~ passed.
- Coverage includes the new regression ~rebuild_ignores_neighbor_dir_locals_files~ plus the existing TODO keyword, CLI JSON, migration, and parser-contract test suites.

** Blockers

- None.

** Follow-ups

- Legacy DB migration cleanup for historical ~dir_locals~ TODO keyword provenance remains in ~src/db.rs~ and ~src/db/schema.rs~ as intentional backwards-compatibility code.
