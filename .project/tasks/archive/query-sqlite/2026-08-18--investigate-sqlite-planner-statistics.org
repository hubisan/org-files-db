#+TITLE: Investigate SQLite Planner Statistics
#+STARTUP: showall
#+TEMPLATE_VERSION: 0.8.0
#+TASK_STARTED: [2026-08-18 Tue 23:30]
#+TASK_COMPLETED:

* Original Task (User only)
:PROPERTIES:
:VISIBILITY: folded
:END:

** DONE Investigate SQLite planner statistics for production queries

Task file: [[./2026-08-18--investigate-sqlite-planner-statistics.org][Investigate SQLite Planner Statistics]]

Start this task after the bundled SQLite runtime and enrichment-aggregation investigations are settled.

Use the final production query implementation and one stable SQLite runtime as the baseline.

SQLite can use persistent planner statistics to choose indexes and join orders.

The current controlled query benchmarks should therefore test whether planner statistics provide a useful production benefit for orgfdb data distributions.

Goal: determine whether ~ANALYZE~ or ~PRAGMA optimize~ should be part of the orgfdb database lifecycle.

This is an investigation task.

Do not enable automatic production optimization before benchmark evidence exists.

Do not change query semantics.

Do not add new indexes in this task.

*** Phase 1 - Establish comparable database states

Create comparable benchmark databases with identical indexed data.

Compare at least:

- the normal current database state
- the same database after the selected SQLite statistics operation

Record the resulting relevant ~sqlite_stat*~ state where practical.

Use the same SQLite runtime for both cases.

Do not compare statistics results across different SQLite versions in this task.

*** Phase 2 - Benchmark representative queries

Measure workloads where planner decisions can matter.

Include:

- cheap heading queries
- selective metadata predicates
- less selective metadata predicates
- expensive multi-predicate queries
- queries that use the selective shared TEMP strategy
- representative enrichment-heavy queries

Record:

- complete query latency
- query plan
- relevant statement counts
- matched result counts

Use repeated samples.

If a query plan changes, record whether the change improves or regresses complete query latency.

Do not infer a benefit only from a query plan that looks better.

*** Phase 3 - Decide lifecycle behavior

If planner statistics provide a clear and stable benefit, determine the smallest safe lifecycle integration.

Consider when statistics would need refreshing after substantial index changes.

Do not run expensive statistics work after every small update unless benchmarks justify it.

Prefer SQLite's supported optimization mechanism over custom statistics management when it provides the required behavior.

If statistics provide no useful production benefit, keep the current lifecycle unchanged.

Document the no-change result.

Update ~docs/query-sql-performance.org~ with:

- tested database states
- SQLite runtime used
- statistics operation used
- changed query plans
- complete query latency results
- the final lifecycle decision

Acceptance criteria:

- Statistics are tested on identical data with one SQLite runtime.
- Representative selective and non-selective queries are included.
- Query-plan changes are recorded where they occur.
- Complete query latency determines the decision.
- No new indexes or compiler changes are mixed into the experiment.
- Any retained lifecycle integration has measured production benefit.
- Statistics refresh behavior is explicit if retained.
- A no-change result is allowed.
- ~make ci~ passes.

* Planning

** Problem summary

The production database lifecycle does not run ~ANALYZE~ or ~PRAGMA optimize~.

Schema creation creates the production indexes but does not create planner statistics.

A full rebuild can replace all indexed Org data without refreshing planner statistics.

Incremental indexing can also change table and index distributions without refreshing planner statistics.

The bundled SQLite runtime enables ~SQLITE_ENABLE_STAT4~.

The task must determine whether planner statistics improve real production queries enough to justify lifecycle work.

** Goals

- Compare the current no-statistics baseline with the same indexed data after a statistics operation.
- Require exact public query-result equality before timing each workload.
- Measure complete production query latency and query plans.
- Include cheap, selective, non-selective, expensive, and enrichment-heavy production queries.
- Record relevant ~sqlite_stat1~ and ~sqlite_stat4~ state.
- Measure the observed cost and database-size change of each tested statistics operation.
- Decide whether the production database lifecycle needs a statistics refresh policy.

** Non-goals

- Do not change public query output or error behavior.
- Do not add indexes.
- Do not change the query compiler.
- Do not update dependencies.
- Do not add production ~ANALYZE~ or ~PRAGMA optimize~ before benchmark evidence exists.
- Do not expand the experiment if the first gate gives a clear no-change result.

** Architecture

Use a dedicated measurement-only benchmark entry point in the existing query SQL benchmark module.

Keep the normal query SQL benchmark at output schema version ~13~.

The first experiment uses one prepared ~50'000~-result production benchmark database.

Create two benchmark copies from identical seeded data:

- baseline: current database state with no planner statistics
- candidate: the same database after full ~ANALYZE~

Full ~ANALYZE~ is the first gate because the bundled runtime enables ~SQLITE_ENABLE_STAT4~.

This gate tests the strongest available statistics state before lifecycle integration work starts.

If full ~ANALYZE~ gives no material benefit, stop the statistics experiment unless the data gives a specific reason to test another state.

If full ~ANALYZE~ gives a clear benefit, test ~PRAGMA optimize~ next as the lifecycle candidate.

SQLite recommends ~PRAGMA optimize~ for application-managed statistics refreshes.

The later comparison must include its bounded analysis cost and its resulting statistics state.

The experiment runner uses a neutral candidate state for this comparison.

Planner-statistics output schema version ~3~ records either ~ANALYZE~ or ~PRAGMA optimize~ as that candidate.

Schema version ~3~ also records downstream heading-tag and outline-validation plans for cheap non-TEMP heading relations.

** Repository audit

~open_database_with_schema~ configures the writable SQLite connection and applies the schema.

~SchemaDefinition::apply~ creates the schema and production indexes.

Neither path runs ~ANALYZE~ or ~PRAGMA optimize~.

The full rebuild replaces indexed Org data and resolves links before commit.

The incremental indexer can replace, add, or delete substantial indexed data before commit.

Neither indexing path refreshes planner statistics.

The query CLI opens the existing database read-only for query execution.

The production query compiler uses predicate-driven metadata relations.

The production result path uses selective shared TEMP matched relations for expensive reused heading queries.

The normal query SQL benchmark remains output schema version ~13~.

It already records SQLite runtime identity, compile options, capabilities, statement profiles, and query plans.

It also has deterministic metadata-selectivity seed data and paired timing support.

** SQLite documentation findings

The official [[https://www.sqlite.org/lang_analyze.html][SQLite ANALYZE documentation]] says that statistics are optional planner input.

Statistics from ~ANALYZE~ are not refreshed automatically when database content changes.

The bundled build reports ~ENABLE_STAT4~.

Full ~ANALYZE~ can therefore create both ~sqlite_stat1~ and ~sqlite_stat4~ data.

SQLite cannot compute ~sqlite_stat4~ with a non-zero analysis limit.

The official [[https://www.sqlite.org/pragma.html#pragma_optimize][PRAGMA optimize documentation]] recommends ~PRAGMA optimize~ for current applications.

SQLite recommends it before closing short-lived database connections and after schema changes, especially after ~CREATE INDEX~.

Since SQLite 3.46.0, ~PRAGMA optimize~ uses a temporary analysis limit when it runs analysis work.

Its decision can also depend on query history from the current connection.

This behavior matters because orgfdb query connections are short-lived and read-only.

** Execution plan

1. Add a dedicated planner-statistics benchmark binary.
2. Reuse the current presentation benchmark corpus and database preparation.
3. Reject a baseline database that already contains ~sqlite_stat1~ or ~sqlite_stat4~.
4. Seed deterministic benchmark-only metadata selectivity rows into one baseline copy.
5. Copy that baseline so both database states contain identical indexed data.
6. Open benchmark write copies with the production WAL and synchronous settings.
7. Run only the selected statistics operation on the candidate copy.
8. Use full ~ANALYZE~ for the first gate.
9. Use normal ~PRAGMA optimize;~ only after the first gate justifies it.
10. Record statistics tables, ~sqlite_stat1~ entries, ~sqlite_stat4~ index sample counts, operation time, and database size.
11. Compare five production workloads with paired sample ordering.
12. Require exact public ~QueryResponse~ equality before and during timing.
13. Record complete latency, statement profiles, compiled match plans, result counts, and TEMP-relation classification.
14. Review the evidence before any production lifecycle change.

** Risks & assumptions

The uploaded repository archive has no ~.git~ directory.

Branch state cannot be inspected from this archive, so this patch does not perform branch operations.

The current environment has no Rust toolchain.

The user will run the release benchmark and ~make ci~ locally.

A reused benchmark work directory can contain manual statistics from an earlier experiment.

The planner-statistics benchmark rejects that state instead of silently using it as the baseline.

The first ~ANALYZE~ cost is one observed operation, not the final lifecycle cost model.

If query gains justify more work, a later step will measure lifecycle cost with repeated database copies.

** Open questions

No open implementation question remains.

The final lifecycle uses normal ~PRAGMA optimize;~ after successful write-side index changes.

Read-only query connections do not run planner maintenance.

** Proposed checks

- Run ~cargo fmt --all~.
- Run ~make ci~.
- Run the targeted planner-statistics lifecycle test through the normal test suite.
- Do not run another planner-statistics benchmark unless review finds a concrete regression.

* Build

The production integration uses normal ~PRAGMA optimize;~ only on writable indexing paths.

A full rebuild runs it after the rebuild transaction commits.

Incremental reconciliation runs it after commit only when the plan contains an invalidation, metadata-only update, create, modify, or delete.

A no-op incremental reconcile does not run it.

Read-only query connections do not run it.

The hook does not force ~ANALYZE~. SQLite decides whether ~PRAGMA optimize;~ needs analysis work.

The watcher keeps one writable connection open for its runtime. The incremental hook therefore runs after each committed watcher update instead of at watcher shutdown.

Planner maintenance is best-effort after commit. A maintenance failure must not report an already committed index update as failed.

The temporary planner-statistics benchmark binary and experiment-only support code are removed after the decision.

* Results

** Approach

The investigation first used an isolated full-~ANALYZE~ gate.

It then compared normal ~PRAGMA optimize;~ on identical data.

The final v4 run added downstream query plans and kept exact public output checks.

The production implementation retains only normal ~PRAGMA optimize;~ at write-side lifecycle points.

The temporary experiment harness is removed.

** Changed files

The first patch changed:

- ~.project/tasks/todo.org~
- ~.project/tasks/2026-08-18--investigate-sqlite-planner-statistics.org~
- ~src/query_sql_benchmark.rs~
- ~src/bin/orgfdb-query-planner-statistics-benchmark.rs~

The optimize-candidate patch changes only:

- ~.project/tasks/2026-08-18--investigate-sqlite-planner-statistics.org~
- ~src/query_sql_benchmark.rs~
- ~src/bin/orgfdb-query-planner-statistics-benchmark.rs~

The downstream-plan verification patch changes only:

- ~.project/tasks/2026-08-18--investigate-sqlite-planner-statistics.org~
- ~src/query_sql_benchmark.rs~

The production integration changes:

- ~.project/tasks/todo.org~
- ~.project/tasks/2026-08-18--investigate-sqlite-planner-statistics.org~
- ~docs/query-sql-performance.org~
- ~src/indexer.rs~
- ~src/query_sql_benchmark.rs~

It removes:

- ~src/bin/orgfdb-query-planner-statistics-benchmark.rs~

** Checks performed

Repository instructions, schema creation, index creation, full rebuild, incremental indexing, query execution, and benchmark code were inspected.

Official SQLite documentation for ~ANALYZE~, ~PRAGMA optimize~, query planning, and ~SQLITE_ENABLE_STAT4~ was reviewed.

The final production diff was checked for remaining experiment-only Rust references.

The temporary benchmark binary is removed, and the retained query SQL benchmark remains schema version ~13~.

** Test results

Earlier benchmark runs are historical investigation data.

The final production decision does not compare their absolute timings with the final WSL2 run.

The local release benchmark completed after the v2 lifetime correction.

The full-~ANALYZE~ candidate used SQLite 3.53.2 with ~ENABLE_STAT4~.

The operation took 641.8 ms on the 50'000-result benchmark database.

The logical database size increased by 94'208 bytes.

It created 41 ~sqlite_stat1~ rows and 787 ~sqlite_stat4~ rows.

No recorded matched-relation query plan changed.

Median complete query latency changed as follows:

- ~cheap.heading-level~: 417.6 ms -> 293.8 ms (-29.65 %)
- ~selective.direct-property-1~: 3.29 ms -> 3.35 ms (+1.69 %)
- ~nonselective.effective-property-100~: 400.9 ms -> 401.7 ms (+0.19 %)
- ~expensive.multi-metadata-10~: 150.0 ms -> 148.2 ms (-1.19 %)
- ~enrichment-heavy.heading-level~: 1619.0 ms -> 1633.6 ms (+0.90 %)

The result is mixed.

One representative 50'000-result query has a large complete-latency gain.

The other four workloads have no material complete-latency gain.

This evidence does not justify full ~ANALYZE~ in production.

It also does not justify rejecting planner statistics yet.

The gate therefore permits one final lifecycle-candidate experiment with normal ~PRAGMA optimize;~.

SQLite recommends ~PRAGMA optimize~ for application-managed statistics.

Since SQLite 3.46.0, normal ~PRAGMA optimize~ applies a temporary analysis limit when it runs analysis work.

A non-zero analysis limit does not generate ~sqlite_stat4~.

The ~PRAGMA optimize~ experiment completed with the same five workloads and exact-output checks.

The operation took 38.3 ms on the 50'000-result benchmark database.

The logical database size increased by 8'192 bytes.

It created 39 ~sqlite_stat1~ rows and no ~sqlite_stat4~ rows.

No recorded matched-relation query plan changed.

Median complete query latency changed as follows:

- ~cheap.heading-level~: 415.1 ms -> 292.1 ms (-29.63 %)
- ~selective.direct-property-1~: 3.25 ms -> 3.28 ms (+0.85 %)
- ~nonselective.effective-property-100~: 400.4 ms -> 398.0 ms (-0.60 %)
- ~expensive.multi-metadata-10~: 152.2 ms -> 151.1 ms (-0.76 %)
- ~enrichment-heavy.heading-level~: 1604.2 ms -> 1623.9 ms (+1.23 %)

This result repeats the large simple-heading gain from full ~ANALYZE~.

The other four workloads remain effectively neutral.

The cheaper operation retains the only material gain without ~sqlite_stat4~.

This result makes ~PRAGMA optimize~ a viable lifecycle candidate.

The current plan evidence is incomplete.

The benchmark records the matched-relation plan, but that plan does not change for the improved workload.

The SQL profile places the improvement in ~query-heading-tags~ and ~validate-outline-path~.

Add one final plan-only extension for those downstream statements on cheap non-TEMP heading relations.

Do not add another statistics candidate.

The final v4 run was executed inside WSL2.

Use only the paired baseline-versus-candidate comparison inside that run for the final decision.

Do not compare its absolute timings with earlier runs from a different environment.

Normal ~PRAGMA optimize;~ took 40.2 ms.

It added 8'192 bytes, created 39 ~sqlite_stat1~ rows, and created no ~sqlite_stat4~ samples.

The paired median complete-query changes were:

- ~cheap.heading-level~: 316.0 ms -> 248.7 ms (-21.29 %)
- ~selective.direct-property-1~: 2.54 ms -> 2.19 ms (-13.74 %)
- ~nonselective.effective-property-100~: 262.8 ms -> 253.9 ms (-3.41 %)
- ~expensive.multi-metadata-10~: 120.8 ms -> 125.5 ms (+3.89 %)
- ~enrichment-heavy.heading-level~: 1306.6 ms -> 1287.9 ms (-1.43 %)

All exact public query-output checks passed.

The matched-relation plans did not change.

The additional heading-tag and outline-validation plans also did not change for the cheap non-TEMP workloads.

The end-to-end result still supports normal ~PRAGMA optimize;~ because four of five representative medians improved, its measured maintenance cost was small, and SQLite recommends this maintenance mechanism.

The production decision does not use full ~ANALYZE~ or explicit ~sqlite_stat4~ maintenance.

Run normal ~PRAGMA optimize;~ after a successful full rebuild commit.

Run it after a successful incremental commit only when indexed state changed.

Do not run it on read-only query connections or no-op watcher batches.

** Blockers

The current environment has no Rust toolchain, so ~make ci~ was not run here.

** Follow-ups

Run ~cargo fmt --all~ and ~make ci~ locally.

No further planner-statistics benchmark is required before review.
