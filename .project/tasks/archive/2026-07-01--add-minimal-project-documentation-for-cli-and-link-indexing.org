#+TITLE: Add minimal project documentation for CLI and link indexing
#+STARTUP: showall
#+TEMPLATE_VERSION: 0.7.0
#+TASK_STARTED: [2026-07-01 Wed 13:52]
#+TASK_COMPLETED:

* Input & Todo Task
:PROPERTIES:
:VISIBILITY: folded
:END:

Copied task entry:

** DONE DOCS: Add minimal project documentation for CLI and link indexing
:PROPERTIES:
:RISK: Low
:DB_SCHEMA_CHANGE: No
:API_CONTRACT_CHANGE: No
:END:

[[./archive/2026-07-01--add-minimal-project-documentation-for-cli-and-link-indexing.org]]

Goal:

- Start small project documentation for user-facing CLI behavior and Phase 3 link indexing decisions.
- Document ~orgfdb links --json~ as the first concrete CLI reference entry.
- Keep documentation lightweight and repository-local for now.

Decision:

- Use Markdown files in ~docs/~ for now.
- Use rustdoc for Rust API and internal code documentation.
- Do not introduce mdBook yet.
- Treat mdBook and GitHub Pages as a later publishing step if the documentation grows into a user guide.

Rationale:

- ~rustdoc~ is best for documenting Rust APIs, structs, traits, modules, and developer-facing code contracts.
- ~mdBook~ is best for manuals, tutorials, guides, and larger user-facing documentation sites.
- The project currently needs a small CLI/reference document more than a full documentation site.
- Plain Markdown in ~docs/~ is easy to maintain and can later be moved into mdBook with little effort.

Suggested structure:

#+begin_src text
README.md
docs/
  cli.md
  link-indexing.md
  development.md
#+end_src

Scope:

- Add or update ~docs/cli.md~.
- Add a section for ~orgfdb links --json~.
- Document command purpose.
- Document that the command is read-only.
- Document that the command reads stored database facts only.
- Document that the command does not scan Org files.
- Document that the command does not rebuild files.
- Document that the command does not resolve targets.
- Document that the command does not mutate the database.
- Document output ordering.
- Document public JSON fields.
- Document root link behavior.
- Link from ~README.md~ to ~docs/cli.md~ if a docs section already exists or if this is easy to add.

Documented command:

#+begin_src shell
orgfdb links --json
#+end_src

Required documentation points:

- The command outputs stored link source facts as JSON.
- The command is a database inspection command.
- The command does not analyze current file contents.
- Root links are included.
- Root links use ~heading_level = 0~.
- Root links use ~heading_path = []~.
- Output uses ~link_type~, not ~type~.
- Output is ordered by:
  - ~file_path~
  - ~byte_start~
  - ~id~

Public JSON fields to document:

- ~file_id~
- ~file_path~
- ~heading_id~
- ~heading_path~
- ~heading_level~
- ~source_context~
- ~format~
- ~link_type~
- ~raw~
- ~raw_target~
- ~raw_description~
- ~path~
- ~search_option~
- ~byte_start~
- ~byte_end~
- ~line~

Non-goals:

- Do not introduce mdBook in this task.
- Do not configure GitHub Pages in this task.
- Do not write a full user manual.
- Do not document unresolved future link relationship behavior.
- Do not document target resolution as existing behavior.
- Do not duplicate large test fixtures in documentation.
- Do not make documentation depend on generated CLI output unless there is already a stable snapshot workflow.

Acceptance criteria:

- [ ] ~docs/cli.md~ exists or is updated.
- [ ] ~orgfdb links --json~ is documented.
- [ ] Documentation states that the command is read-only.
- [ ] Documentation states that it reads stored database facts only.
- [ ] Documentation states that it does not scan, rebuild, resolve targets, or mutate the database.
- [ ] Documentation lists the public JSON fields.
- [ ] Documentation states that ~link_type~ is used instead of ~type~.
- [ ] Documentation describes deterministic output ordering.
- [ ] Documentation describes root link behavior.
- [ ] README links to ~docs/cli.md~ if appropriate.
- [ ] No mdBook or GitHub Pages setup is added yet.

Follow-up candidates:

- Add rustdoc comments for public Rust APIs that expose link rows, scanner structs, or CLI DTOs.
- Add ~docs/link-indexing.md~ for the Phase 3 scanner contract and known parser mismatches.
- Introduce mdBook only after the docs grow beyond a few Markdown files.
- Publish docs through GitHub Pages only when there is a stable user-facing manual.

* Results

** Approach
Added a lightweight Markdown CLI reference at ~docs/cli.md~ and a separate config reference at ~docs/config.md~, then linked both from the repository README. The CLI page now documents ~orgfdb headings --json~ and ~orgfdb links --json~ as read-only database inspection commands, while the config page lists the supported ~config.toml~ variables and defaults. The scope stayed repository-local and did not introduce mdBook.

** Changed files
- `docs/cli.md`
- `docs/config.md`
- `README.org`
- `.project/tasks/todo.org`
- `.project/tasks/2026-07-01--add-minimal-project-documentation-for-cli-and-link-indexing.org`

** Checks performed
- `make ci`

** Test results
- Repository checks passed.
- No code-path changes were introduced by this docs task.
- `orgfdb headings --json` and `orgfdb links --json` are both documented in the new CLI reference.
- Supported `config.toml` variables are documented in the new config reference.

** Blockers
None.

** Follow-ups
If documentation grows, add `docs/link-indexing.md` next and defer mdBook until the docs need a real manual structure.
