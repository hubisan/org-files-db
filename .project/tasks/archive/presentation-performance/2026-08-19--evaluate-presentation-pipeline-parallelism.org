#+TITLE: Evaluate Presentation Pipeline Parallelism
#+STARTUP: showall
#+TEMPLATE_VERSION: 0.8.0
#+TASK_STARTED: [2026-08-19 Wed 16:06]
#+TASK_COMPLETED:

* Original Task (User only)
:PROPERTIES:
:VISIBILITY: folded
:END:

Start this task after presentation payload optimization is complete.

Use the final sequential presentation pipeline as the baseline.

Goal: add parallel execution only where profiling shows useful CPU-bound work.

A valid result for this task is to keep the complete pipeline sequential.

Profile the sequential pipeline before adding a dependency.

Measure stages such as:

- value extraction
- row expansion
- sort-key creation
- sorting
- width reduction
- truncation and padding
- final row formatting
- serialization where applicable

Keep the pipeline suitable for parallel execution:

- Prefer immutable shared input.
- Keep row processing independent.
- Avoid shared mutable state between rows.
- Build extracted values locally.
- Build sort keys locally.
- Format each row independently after shared widths are known.

Do not parallelize SQLite query execution by default.

Keep one database snapshot for each presentation response.

Do not add ~rayon~ or another dependency before benchmarks identify a suitable bottleneck.

If a candidate stage is expensive enough, compare sequential and parallel implementations.

Use identical benchmark inputs.

Require exact output equality.

Keep result ordering deterministic.

Measure:

- stage latency
- complete presentation latency
- total CLI latency
- small-workload overhead
- large-workload improvement

If parallel startup and coordination cost dominates, keep that stage sequential.

If useful only above a workload threshold, use a simple deterministic threshold.

Document the threshold and its benchmark evidence.

Do not change:

- query semantics
- presentation ordering
- column semantics
- ~result_index~ references
- ~database_id~ semantics
- ~generation~ semantics
- normal ~--format json~ compatibility

Acceptance criteria:

- Baseline profiling identifies the expensive presentation stages.
- Parallel execution targets only measured CPU bottlenecks.
- Sequential and parallel execution produce identical output.
- Result ordering remains deterministic.
- Small workloads have no material regression.
- Enabled parallel paths show a measurable large-workload improvement.
- The implementation keeps sequential execution where parallel work loses.
- A fully sequential final design is allowed when benchmarks reject parallelism.
- Benchmark results record the final execution strategy.
- ~make ci~ passes.


** DONE Evaluate presentation pipeline parallelism
:PROPERTIES:
:VISIBILITY: folded
:END:

Task file: [[./2026-08-19--evaluate-presentation-pipeline-parallelism.org][Evaluate Presentation Pipeline Parallelism]]

Start this task after presentation payload optimization is complete.

Use the final sequential presentation pipeline as the baseline.

Goal: add parallel execution only where profiling shows useful CPU-bound work.

A valid result for this task is to keep the complete pipeline sequential.

Profile the sequential pipeline before adding a dependency.

Measure stages such as:

- value extraction
- row expansion
- sort-key creation
- sorting
- width reduction
- truncation and padding
- final row formatting
- serialization where applicable

Keep the pipeline suitable for parallel execution:

- Prefer immutable shared input.
- Keep row processing independent.
- Avoid shared mutable state between rows.
- Build extracted values locally.
- Build sort keys locally.
- Format each row independently after shared widths are known.

Do not parallelize SQLite query execution by default.

Keep one database snapshot for each presentation response.

Do not add ~rayon~ or another dependency before benchmarks identify a suitable bottleneck.

If a candidate stage is expensive enough, compare sequential and parallel implementations.

Use identical benchmark inputs.

Require exact output equality.

Keep result ordering deterministic.

Measure:

- stage latency
- complete presentation latency
- total CLI latency
- small-workload overhead
- large-workload improvement

If parallel startup and coordination cost dominates, keep that stage sequential.

If useful only above a workload threshold, use a simple deterministic threshold.

Document the threshold and its benchmark evidence.

Do not change:

- query semantics
- presentation ordering
- column semantics
- ~result_index~ references
- ~database_id~ semantics
- ~generation~ semantics
- normal ~--format json~ compatibility

Acceptance criteria:

- Baseline profiling identifies the expensive presentation stages.
- Parallel execution targets only measured CPU bottlenecks.
- Sequential and parallel execution produce identical output.
- Result ordering remains deterministic.
- Small workloads have no material regression.
- Enabled parallel paths show a measurable large-workload improvement.
- The implementation keeps sequential execution where parallel work loses.
- A fully sequential final design is allowed when benchmarks reject parallelism.
- Benchmark results record the final execution strategy.
- ~make ci~ passes.

* Planning

** Problem summary

The final presentation pipeline is sequential.

The completed payload task provides a fresh sequential production benchmark for ~presentation-json~ version 2.

At 50,000 original results, the expensive CPU stages differ by workload.

Expanded 150,000-row workloads spend the largest isolated stage time in sorting:

- ~headings.tags~: 219.10 ms median sorting
- ~headings.effective-properties~: 301.79 ms median sorting
- ~files.keywords~: 264.35 ms median sorting

The wide 50,000-row workload spends substantial time in value extraction and final formatting:

- ~headings.wide~: 51.87 ms median value extraction
- ~headings.wide~: 64.53 ms median truncation, padding, and row formatting

The baseline therefore identifies CPU-bound work before any parallel dependency is added.

** Goals

- Compare sequential and parallel work on identical in-memory query results.
- Test sort-key creation and sorting as one candidate area.
- Test value extraction with width reduction as one candidate area.
- Test final row formatting as one candidate area.
- Measure complete presentation latency for sort-only, layout-only, and combined candidates.
- Require exact ~PresentationResponse~ equality for every candidate.
- Measure 100, 1,000, 10,000, and 50,000 original results.
- Use the final version 2 presentation wire implementation as the baseline.

** Non-goals

- Do not parallelize SQLite query execution.
- Do not change presentation semantics or ordering.
- Do not change the production execution strategy in the first experiment.
- Do not add parallel JSON serialization.
- Do not add a cache or streaming path.

** Architecture

Use Rayon only in benchmark-only candidate methods during the first experiment.

The candidate keeps row expansion sequential because the baseline does not identify it as a dominant stage.

The parallel sort candidate performs sort-key extraction per row in parallel and uses a parallel stable sort with the existing comparator.

The comparator still uses ~original_index~ as the final key.

This preserves deterministic ordering for equal presentation sort values.

The parallel layout candidate extracts cells independently per row.

Each worker computes local natural widths for its row.

A deterministic sequential reduction combines the local widths after extraction.

Final row formatting mutates only the cells in one row and can run independently after widths are known.

The production ~build_response~ method remains sequential during the experiment.

** Execution plan

1. Add an isolated parallel-candidate benchmark that reuses existing benchmark databases.
2. Compare sequential and parallel stage latency on the same query results.
3. Compare sequential, sort-only, layout-only, and combined complete presentation latency.
4. Validate exact output equality before timing results are accepted.
5. Run the candidate benchmark at 100, 1,000, 10,000, and 50,000 original results.
6. Select only stages that improve large workloads without material small-workload cost.
7. If needed, define one deterministic row-count threshold from the benchmark evidence.
8. Integrate only the selected strategy into production.
9. Run the normal presentation benchmark again to measure final stage, presentation, serialization, and CLI latency.
10. Remove experiment-only code if it is not part of the final design.

** Risks & assumptions

The first parallel benchmark uses the global Rayon pool.

Its warm-up samples exclude one-time pool initialization from stage medians.

The final CLI benchmark must still include real process startup and first-use costs.

Parallel cell extraction creates per-row local width data before the deterministic width reduction.

The benchmark must reject this candidate if that allocation cost removes the CPU benefit.

Separate benchmark runs can contain operating-system noise.

The candidate benchmark therefore compares sequential and parallel implementations in one process on identical in-memory inputs.

** Open questions

- Does parallel sorting improve the 150,000-row expanded workloads enough to justify Rayon?
- Does parallel layout work improve ~headings.wide~ and the expanded workloads?
- Does combining parallel sort and layout work improve complete presentation latency?
- Which row-count threshold avoids small-workload overhead?
- Does the final CLI path retain enough of the isolated gain after Rayon pool startup?

** Proposed checks

- ~cargo fmt --all~
- ~make ci~
- Build the isolated parallel benchmark in release mode.
- Run 100, 1,000, 10,000, and 50,000 result workloads against the existing final version 2 benchmark databases.
- Require exact sequential and parallel response equality.
- Record available CPU count and Rayon worker count.

* Results

** Approach

The final sequential production benchmark from the payload task is the baseline.

The task first tested benchmark-only parallel candidates.

The corrected paired v3 benchmark selected a conservative strategy.

Production v4 integrates only the selected paths and keeps the remaining stages sequential.

** Changed files

The first parallel-candidate patch changes:

- ~.project/tasks/todo.org~
- ~.project/tasks/2026-08-19--evaluate-presentation-pipeline-parallelism.org~
- ~Cargo.toml~
- ~docs/presentation-performance.org~
- ~src/lib.rs~
- ~src/presentation.rs~
- ~src/presentation_parallel_benchmark.rs~
- ~src/bin/orgfdb-presentation-parallel-benchmark.rs~

Cargo generates ~Cargo.lock~ entries when it resolves the new benchmark dependency.

The production integration additionally changes:

- ~Cargo.toml~
- ~src/presentation.rs~
- ~src/presentation_benchmark.rs~
- ~src/presentation_parallel_benchmark.rs~
- ~docs/presentation-performance.org~

~Cargo.lock~ already contains the resolved Rayon packages from the benchmark experiment.

** Checks performed

Repository instructions, the final presentation benchmark, and the presentation pipeline were inspected.

The existing baseline already measures row expansion, sort-key creation, sorting, value extraction, width calculation, final formatting, complete presentation latency, serialization, and CLI latency.

** Test results

The v1 candidate benchmark uses 14 available CPUs and 14 Rayon worker threads.

All candidate responses equal the sequential response for every workload and size.

Forced parallel execution loses heavily at 100 and 1,000 results.

At 10,000 results, only ~headings.wide~ shows a strong complete-pipeline gain.

Its layout-only candidate changes complete presentation time by -44.6 %.

The expanded 30,000-row workloads do not show a material complete-pipeline gain at this size.

At 50,000 results, layout-only changes complete presentation time by:

- ~headings.normal~: -10.8 %
- ~headings.wide~: -40.7 %
- ~headings.tags~: +4.9 %
- ~headings.effective-properties~: +11.4 %
- ~files.keywords~: -4.4 %

At 50,000 results, parallel value extraction changes the isolated stage by:

- ~headings.narrow~: +58.2 %
- ~headings.normal~: -60.1 %
- ~headings.wide~: -66.1 %
- ~headings.tags~: -57.5 %
- ~headings.effective-properties~: -40.4 %
- ~files.keywords~: -10.3 %

Parallel formatting helps the large one-row-per-result normal and wide workloads.

It loses for the large expanded tag and effective-property workloads.

Parallel sorting helps the isolated 150,000-row expanded sorting stage by 41.5 % to 59.6 %.

The complete sort-only gain is smaller because sort-key creation and other stages remain significant.

The v1 data does not support one global all-parallel threshold.

** Candidate strategy for v2

The v2 benchmark tests one deterministic selective strategy before production integration.

The strategy uses parallel sorting at 100,000 presentation rows.

It uses parallel value extraction when either condition is true:

- the presentation has at least 200,000 cells
- the presentation has at least 8 columns and 100,000 cells

It uses parallel final formatting only when there is one presentation row per query result and the layout condition is true.

All candidate responses equal the sequential production response.

The v2 stage and isolated-candidate results confirm two useful areas.

At 50,000 original results, layout-only changes complete presentation time by:

- ~headings.normal~: -36.6 %
- ~headings.wide~: -39.3 %

At 10,000 original results, ~headings.wide~ layout-only changes complete presentation time by -22.3 %.

At 50,000 original results, sort-only changes complete presentation time for expanded rows by:

- ~headings.tags~: -3.0 %
- ~headings.effective-properties~: -27.1 %
- ~files.keywords~: -20.0 %

The v2 selective total is not a valid production decision gate.

When all strategy flags are false, the selective path executes the same sequential candidate operations.

Its separately measured median still differs from the sequential total by large amounts in several workloads.

For example, the 10,000-result ~headings.tags~ selective result reports -23.5 % even though all strategy flags are false.

The 1,000-result ~files.keywords~ selective result reports +59.6 % with all strategy flags false.

These differences show run-order and sample noise in the separate total measurements.

Do not use those selective percentages to choose a production strategy.

** Corrected candidate strategy for v3

Use the same candidate implementation for the timed sequential reference and the selective strategy.

Measure these two paths as paired samples and alternate their execution order.

Keep the production ~build_response~ path sequential during this correction.

Use parallel sorting only when the presentation has at least 150,000 rows.

This threshold matches the measured large expanded workloads directly.

The 30,000-row expanded workloads do not show a reliable complete-presentation gain.

Use parallel layout work only for one-row-per-result presentations.

Enable parallel extraction and formatting together when either condition is true:

- the presentation has at least 200,000 cells
- the presentation has at least 8 columns and 100,000 cells

Do not parallelize extraction or formatting for expanded-row presentations in v3.

This keeps the strategy simple and avoids weak or conflicting expanded-layout results.

The corrected v3 benchmark ran on native Linux with 8 available CPUs and 8 Rayon worker threads.

All sequential-candidate and selective-strategy responses equal the semantic reference response.

The paired selective strategy changes complete presentation time by:

- ~headings.wide~ at 10,000 results: -39.2 %
- ~headings.normal~ at 50,000 results: -18.2 %
- ~headings.wide~ at 50,000 results: -39.8 %
- ~headings.tags~ at 50,000 results and 150,000 rows: -14.9 %
- ~headings.effective-properties~ at 50,000 results and 150,000 rows: -8.6 %
- ~files.keywords~ at 50,000 results and 150,000 rows: -11.2 %

Workloads below the selected thresholds keep all strategy flags false.

Their paired selective measurements stay close to the sequential candidate and contain only measurement noise.

The v3 result therefore supports production integration of the conservative strategy.

** Production integration v4

Production ~PresentationSpec::build_response~ now uses the selected strategy.

The implementation uses parallel sort-key creation and parallel stable sorting only when a sorted presentation has at least 150,000 rows.

The implementation uses parallel value extraction, local width collection, and final row formatting only for one-row-per-result presentations that meet one layout threshold:

- at least 200,000 cells
- at least 8 columns and at least 100,000 cells

Expanded-row layout stays sequential.

Row expansion, width resolution, SQLite query execution, and JSON serialization stay sequential.

The production code and the diagnostic benchmark use the same threshold helpers from ~src/presentation.rs~.

Rayon 1.12.0 is now a normal runtime dependency.

The ~presentation-parallel-benchmark~ feature now gates only the diagnostic benchmark module and binary.

The normal presentation benchmark records the active execution strategy for each workload and measures production parallel stages.

** Production validation v4

The production benchmark ran on native Linux with 8 available CPUs.

It uses the final production dispatch and records each workload strategy.

The active strategy matches the corrected v3 selection exactly.

At 10,000 results, only ~headings.wide~ enables parallel extraction and formatting.

At 50,000 results, ~headings.normal~ and ~headings.wide~ enable parallel extraction and formatting.

At 50,000 results, the three 150,000-row expanded workloads enable parallel sorting only.

All smaller workloads and ~headings.narrow~ at 50,000 results stay sequential.

The production presentation medians are:

| Workload | Results | Rows | Production median |
|----------+---------+------+-------------------|
| ~headings.wide~ | 10,000 | 10,000 | 20.21 ms |
| ~headings.narrow~ | 50,000 | 50,000 | 44.70 ms |
| ~headings.normal~ | 50,000 | 50,000 | 61.11 ms |
| ~headings.wide~ | 50,000 | 50,000 | 110.97 ms |
| ~headings.tags~ | 50,000 | 150,000 | 254.35 ms |
| ~headings.effective-properties~ | 50,000 | 150,000 | 342.21 ms |
| ~files.keywords~ | 50,000 | 150,000 | 263.37 ms |

The corresponding complete CLI medians are:

| Workload | Results | CLI median |
|----------+---------+------------|
| ~headings.wide~ | 10,000 | 128.27 ms |
| ~headings.narrow~ | 50,000 | 460.64 ms |
| ~headings.normal~ | 50,000 | 546.88 ms |
| ~headings.wide~ | 50,000 | 657.29 ms |
| ~headings.tags~ | 50,000 | 778.15 ms |
| ~headings.effective-properties~ | 50,000 | 1,023.84 ms |
| ~files.keywords~ | 50,000 | 903.03 ms |

The v4 production presentation totals stay close to the paired v3 candidate totals on the same 8-CPU Linux environment.

The difference ranges from -9.6 % to +7.6 % for the listed 10,000 and 50,000 result workloads.

The 10,000-result wide workload differs by only +1.0 %.

This confirms that the measured candidate gains transfer to the production path.

The older final sequential production benchmark ran with 14 available CPUs.

The v4 production benchmark ran with 8 available CPUs.

Do not use a direct percentage between these two runs as a causal parallel speedup.

Use the paired v3 benchmark for the causal sequential-versus-parallel comparison.

Use v4 as the production dispatch, stage, serialization, and complete CLI validation.

** Blockers

The assistant environment has no Rust toolchain, so it cannot run ~cargo fmt --all~ or ~make ci~.

The production benchmark gate is complete.

An explicit successful local ~make ci~ result remains before review.

** Follow-ups

Confirm that the local ~make ci~ run passes.

After that confirmation, move the task to ~REVIEW~.
