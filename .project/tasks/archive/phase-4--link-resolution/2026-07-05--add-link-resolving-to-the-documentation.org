#+TITLE: Add Link Resolving to the Documentation
#+STARTUP: showall
#+TEMPLATE_VERSION: 0.7.0
#+TASK_STARTED: [2026-07-05 Sun 22:33]
#+TASK_COMPLETED:

* Input & Todo Task
:PROPERTIES:
:VISIBILITY: folded
:END:

- User request summary:
  - Implement the documentation-only BUILD task for current Phase 4 link resolving behavior.
  - Keep docs aligned with the implemented resolver contract and fix the task title typo if present.
  - Cover source facts, resolver-owned fields, resolution statuses, supported and unsupported forms, and =links --json= read-only behavior.
- Active task entry copied from the provided task text:

#+begin_src org
,** BUILD Add Link Resolving to the Documentation
:PROPERTIES:
:PRIORITY: Medium
:RISK: Low
:DB_SCHEMA_CHANGE: No
:API_CONTRACT_CHANGE: No
:END:

Goal:

- Document the current link resolving behavior clearly and accurately.

Scope:

- Documentation only.
- Do not change resolver behavior.
- Do not change parser behavior.
- Do not change DB schema.
- Do not change CLI behavior.
- Keep the docs aligned with the implemented Phase 4 resolver behavior.
- Fix the task title typo if present:
  - =Linke Resolving= -> =Link Resolving=
#+end_src

* Planning

** Problem summary
- The current docs mention link resolution, but the contract is spread across multiple places and does not yet spell out the supported and intentionally unsupported resolver cases with enough precision.

** Goals
- Document the Phase 3 source-fact fields separately from the Phase 4 resolver-owned fields.
- Describe the implemented resolution statuses and their diagnostics.
- Capture the supported resolver forms, the intentional gaps, and the read-only =links --json= behavior.

** Non-goals
- No code behavior changes.
- No schema changes.
- No parser or resolver semantics changes.

** Architecture
- Update =README.org= with a concise high-level resolver contract summary and explicit scope decisions.
- Update =docs/cli.org= with the public =links --json= field list and read-only semantics.
- Keep the wording aligned with the current Phase 4 behavior rather than the earlier Phase 3-only view.

** Execution plan
- Add the task file and link it from =todo.org=.
- Revise the README link-resolution summary.
- Revise the CLI reference for =orgfdb links --json=.
- Run formatter and =make ci= to confirm documentation edits do not disturb the repository.

** Risks & assumptions
- Assumption: the main docs already contain most of the needed content, so this task is primarily a precision and structure pass rather than a broad rewrite.

** Open questions
- None.

** Proposed checks
- =cargo fmt --all=
- =make ci=

* Results

** Approach
- Updated the public link-resolution documentation in =README.org= and =docs/cli.org= without changing any resolver, parser, DB, or CLI behavior.
- Split the docs so the Phase 3 source facts and the Phase 4 resolver-owned fields are described separately, and added explicit scope notes for supported and unsupported link forms.
- Kept the CLI reference focused on the public =links --json= contract, including read-only behavior and the stored resolution states.

** Changed files
- =README.org=
- =docs/cli.org=
- =.project/tasks/todo.org=
- =.project/tasks/2026-07-05--add-link-resolving-to-the-documentation.org=

** Checks performed
- =cargo fmt --all=
- =make ci=

** Test results
- Documentation now describes the implemented resolver contract, the read-only =links --json= output, and the supported/unresolved/unsupported scope decisions.
- =make ci= passed.

** Blockers
- None.

** Follow-ups
- None.
