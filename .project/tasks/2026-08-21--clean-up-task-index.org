#+TITLE: Clean Up Task Index
#+STARTUP: showall
#+TEMPLATE_VERSION: 0.8.0
#+TASK_STARTED: [2026-08-21 Fri 23:59]
#+TASK_COMPLETED:

* Original Task (User only)
:PROPERTIES:
:VISIBILITY: folded
:END:

Keep ~todo.org~ as small as possible.

Move all completed work to ~archive/todo-archive.org~.

Keep the Phase 0-8 history, but add structure to later completed work.

Adjust links after the move.

Keep one short completed-work summary in ~todo.org~.

* Planning

** Goals

- Keep only active work in ~todo.org~.
- Put completed and cancelled task history in one archive index.
- Move completed standalone task files to the archive directory.
- Remove dead links to historical task files that are absent from the repository.
- Keep a short milestone summary in the active index.

** Non-goals

- Do not rewrite historical task results.
- Do not change the roadmap.
- Do not change production code or documentation.

** Proposed checks

- Check that every relative Org link in ~todo.org~ resolves.
- Check that every retained task-file link in ~todo-archive.org~ resolves.
- Check that no completed standalone task file remains in the active task directory.
- If the Rust toolchain is available, run ~make ci~.

* Results

** Approach

The active task index now contains only current work and a short milestone summary.

The old completed task history now lives in ~archive/todo-archive.org~.

Phase 0-8 stays together as the original project sequence.

Later completed work is grouped by repository hardening, presentation, query performance, cache work, and final maintenance.

Completed standalone task files now live beside the archive index.

Dead links to older task files were removed because those files are not present in the supplied repository.

** Changed files

- ~.project/tasks/todo.org~
- ~.project/tasks/archive/todo-archive.org~
- ~.project/tasks/2026-08-21--clean-up-task-index.org~
- Completed standalone task files moved from ~.project/tasks/~ to ~.project/tasks/archive/~.

** Checks performed

- Checked relative links in the active task index.
- Checked retained relative links in the archive index and moved task files.
- Checked that completed standalone task files moved to the archive directory.
- Checked that the active index contains no ~DONE~ or ~CANCEL~ headings.
- Audited the new text with the Simple English pragmatic rules.
- Attempted ~make ci~.

** Test results

Static task-index checks pass.

~make ci~ cannot start because this environment does not provide ~cargo~.

The failure is:

#+begin_example
make: cargo: No such file or directory
make: *** [Makefile:19: fmt] Error 127
#+end_example

** Blockers

Run ~make ci~ in an environment with the Rust toolchain before approval.

** Follow-ups

After approval, archive this maintenance task file. Then remove its ~REVIEW~ entry from ~todo.org~.
