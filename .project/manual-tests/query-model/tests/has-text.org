#+TITLE: Query Model Tests - Properties
#+STARTUP: showall

* Paths
:PROPERTIES:
:VISIBILITY: folded
:END:

#+name: cargo-manifest
: ../../../../Cargo.toml

#+name: config-path
: ../Config.toml

* has-text

** Single String

#+begin_src shell :noweb yes :results raw :wrap SRC js
  cargo run --manifest-path <<cargo-manifest()>> --bin orgfdb -- query --json --config <<config-path()>> '(headings (has-text "single match"))' \
  | jq '.results[] | {
      kind,
      file_path: .location.file_path,
      title_raw,
    }'
#+end_src

#+RESULTS:
#+begin_SRC js
{
  "kind": "heading",
  "file_path": "/home/hubisan/projects/coding/org-files-db/.project/manual-tests/query-model/org/has-text.org",
  "title_raw": "has-text: Single Has Text"
}
#+end_SRC

#+begin_src shell :noweb yes :results raw :wrap SRC js
  cargo run --manifest-path <<cargo-manifest()>> --bin orgfdb -- query --json --config <<config-path()>> '(headings (has-text "multiple matches"))' \
  | jq '.results[] | {
      kind,
      file_path: .location.file_path,
      title_raw,
    }'
#+end_src

#+RESULTS:
#+begin_SRC js
{
  "kind": "heading",
  "file_path": "/home/hubisan/projects/coding/org-files-db/.project/manual-tests/query-model/org/has-text.org",
  "title_raw": "has-text: Multiple Matches 1"
}
{
  "kind": "heading",
  "file_path": "/home/hubisan/projects/coding/org-files-db/.project/manual-tests/query-model/org/has-text.org",
  "title_raw": "has-text: Multiple Matches 2"
}
#+end_SRC

** Multiple Strings

#+begin_src shell :noweb yes :results raw :wrap SRC js
  cargo run --manifest-path <<cargo-manifest()>> --bin orgfdb -- query --json --config <<config-path()>> '(headings (has-text "single" "match"))' \
  | jq '.results[] | {
      kind,
      file_path: .location.file_path,
      title_raw,
    }'
#+end_src

#+RESULTS:
#+begin_SRC js
{
  "kind": "heading",
  "file_path": "/home/hubisan/projects/coding/org-files-db/.project/manual-tests/query-model/org/has-text.org",
  "title_raw": "has-text: Single Has Text"
}
#+end_SRC

Not full word:

#+begin_src shell :noweb yes :results raw :wrap SRC js
  cargo run --manifest-path <<cargo-manifest()>> --bin orgfdb -- query --json --config <<config-path()>> '(headings (has-text "sing" "mat"))' \
  | jq '.results[] | {
      kind,
      file_path: .location.file_path,
      title_raw,
    }'
#+end_src

#+RESULTS:
#+begin_SRC js
{
  "kind": "heading",
  "file_path": "/home/hubisan/projects/coding/org-files-db/.project/manual-tests/query-model/org/has-text.org",
  "title_raw": "has-text: Single Has Text"
}
#+end_SRC

Reverse order:

#+begin_src shell :noweb yes :results raw :wrap SRC js
  cargo run --manifest-path <<cargo-manifest()>> --bin orgfdb -- query --json --config <<config-path()>> '(headings (has-text "match" "single"))' \
  | jq '.results[] | {
      kind,
      file_path: .location.file_path,
      title_raw,
    }'
#+end_src

#+RESULTS:
#+begin_SRC js
{
  "kind": "heading",
  "file_path": "/home/hubisan/projects/coding/org-files-db/.project/manual-tests/query-model/org/has-text.org",
  "title_raw": "has-text: Single Has Text"
}
#+end_SRC

#+begin_src shell :noweb yes :results raw :wrap SRC js
  cargo run --manifest-path <<cargo-manifest()>> --bin orgfdb -- query --json --config <<config-path()>> '(headings (has-text "multiple" "matches"))' \
  | jq '.results[] | {
      kind,
      file_path: .location.file_path,
      title_raw,
    }'
#+end_src

#+RESULTS:
#+begin_SRC js
{
  "kind": "heading",
  "file_path": "/home/hubisan/projects/coding/org-files-db/.project/manual-tests/query-model/org/has-text.org",
  "title_raw": "has-text: Multiple Matches 1"
}
{
  "kind": "heading",
  "file_path": "/home/hubisan/projects/coding/org-files-db/.project/manual-tests/query-model/org/has-text.org",
  "title_raw": "has-text: Multiple Matches 2"
}
#+end_SRC

#+begin_src shell :noweb yes :results raw :wrap SRC js
  cargo run --manifest-path <<cargo-manifest()>> --bin orgfdb -- query --json --config <<config-path()>> '(headings (has-text "matches" "multiple"))' \
  | jq '.results[] | {
      kind,
      file_path: .location.file_path,
      title_raw,
    }'
#+end_src

#+RESULTS:
#+begin_SRC js
{
  "kind": "heading",
  "file_path": "/home/hubisan/projects/coding/org-files-db/.project/manual-tests/query-model/org/has-text.org",
  "title_raw": "has-text: Multiple Matches 1"
}
{
  "kind": "heading",
  "file_path": "/home/hubisan/projects/coding/org-files-db/.project/manual-tests/query-model/org/has-text.org",
  "title_raw": "has-text: Multiple Matches 2"
}
#+end_SRC

** Regexp

#+begin_src shell :noweb yes :results raw :wrap SRC js
  cargo run --manifest-path <<cargo-manifest()>> --bin orgfdb -- query --json --config <<config-path()>> '(headings (has-text "([abcdm]ultiple|single)+.*tch" :regexp t))' \
  | jq '.results[] | {
      kind,
      file_path: .location.file_path,
      title_raw,
    }'
#+end_src

#+RESULTS:
#+begin_SRC js
{
  "kind": "heading",
  "file_path": "/home/hubisan/projects/coding/org-files-db/.project/manual-tests/query-model/org/has-text.org",
  "title_raw": "has-text: Single Has Text"
}
{
  "kind": "heading",
  "file_path": "/home/hubisan/projects/coding/org-files-db/.project/manual-tests/query-model/org/has-text.org",
  "title_raw": "has-text: Multiple Matches 1"
}
{
  "kind": "heading",
  "file_path": "/home/hubisan/projects/coding/org-files-db/.project/manual-tests/query-model/org/has-text.org",
  "title_raw": "has-text: Multiple Matches 2"
}
#+end_SRC
